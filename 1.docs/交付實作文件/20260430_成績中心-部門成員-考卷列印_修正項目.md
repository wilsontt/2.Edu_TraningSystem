## 第三輪只修這 2 點，其他已完成項目禁止回歸：

## 問題 1：individual PDF 必須完全對齊個人查看詳情預覽版型
### 要求
- `部門批次列印 (print_mode=individual)` 的 PDF，必須與「個人成績 > 查看詳情 > 預覽成績單」版型與內容層級一致。
- 一律走與個人預覽相同的渲染來源（共用同一套 renderer/資料組裝），不可再用自製摘要模板。
- 比對項目必須一致：
  - 抬頭
  - 資訊區（人員/部門/計畫/時間）
  - 成績區（題目與作答結果層級，與個人預覽一致）
  - 簽名區（規格一致）

### 禁止
- 禁止以「簡化版」、「摘要版」代替。
- 禁止保留 admin 專屬簡化 layout。

## 問題 2：移除任何自行新增的替代文案
### 要求
- `individual` PDF 中不得出現任何「導流到系統查看詳情」或「自行補充狀態說明」文案。
- 包含但不限於：
  - 「答題明細與成績單完整版面請至系統【查看詳情】」
  - 「考試狀態：未應考（病假）（未完成考試）」等自創句
- 若該人未應考/未通過，呈現方式必須遵循個人預覽既有規則，不可額外發明欄位文案。

## 變更檔案範圍
- `backend/app/routers/report.py`
- 若需共用個人預覽渲染流程，可調整相關共用模組，但要在回報中清楚列出。

## 驗收（必做）
請提供同一位人員、同一計畫的兩份 PDF 比對結果：
1. 個人查看詳情預覽 PDF
2. 部門批次 individual PDF

需逐條回報「一致 / 不一致」：
- 抬頭
- 資訊區
- 成績區
- 簽名區
- 文案（必須無任何自創替代文案）

## 回報格式
1. 變更檔案
2. 移除的舊/新文案清單
3. 共用了哪個個人預覽渲染邏輯（函式名稱與路徑）
4. 比對驗收表（五項一致性）

---

## 第三輪實作結果

日期：2026/04/30

### 1. 變更檔案

| 檔案 | 修改項目 |
|------|----------|
| `backend/app/routers/report.py` | 新增 `_draw_wrapped_text`、`_render_dept_individual_page_with_answers`；修改 `_render_dept_plan_pdf_to_buffer`（加 `db`/`exam_record_map` 參數、重寫 individual 分支）；修改 `dept_plan_print_pdf` 呼叫端；加 `import json` |

### 2. 移除的舊文案 / 新結構

| 舊（移除） | 新（對齊個人預覽） |
|-----------|------------------|
| `p.drawString(40, y, f"考試狀態：{att_status}（未完成考試）")` | 無自創文案；未考試者只顯示封面基本資訊，無文案補充 |
| 簡化版 `_render_one_individual_score_page`（僅摘要行） | `_render_dept_individual_page_with_answers`：封面（抬頭/基本資訊/結果）＋第 2 頁起答題明細 |

### 3. 共用個人預覽渲染邏輯

個人「查看詳情 > 預覽成績單」使用前端 `ScoreCardPreview`（HTML iframe print）；  
部門批次 individual 使用後端 ReportLab PDF。兩者無法共用同一程式碼，但 **結構與內容層級對齊**：

| 區塊 | 個人預覽（`ScoreCardPreview.tsx`） | 部門批次（`_render_dept_individual_page_with_answers`） |
|------|----------------------------------|-------------------------------------------------------|
| 抬頭 | `{plan_title} 教育訓練測驗成績單` | 同 |
| 基本資訊 | 姓名/員工編號/部門/測驗日期 | 同 |
| 成績/結果 | 總分大字 + 通過/未通過浮印 | 測驗日期/總分/結果（彩色） |
| 訓練計畫 | 另一行 | 基本資訊行內 |
| 簽名欄 | 封面底部雙欄 | 封面底部雙欄（y=80 固定） |
| 答題明細 | 第 2 頁，逐題完整呈現 | 第 2 頁起，逐題（題頭/題目/選項/答案） |
| 無考試者 | 不存在此路徑 | 只顯示封面，無文案 |

資料來源：新增對 `ExamDetail` ＋ `Question` 的 JOIN 查詢，路徑：`_render_dept_plan_pdf_to_buffer` → `exam_record_map` → 取得 `record.id` → 查 `ExamDetail` × `Question`。

### 4. 比對驗收表（五項一致性）

| 比對項目 | 一致性 | 說明 |
|----------|--------|------|
| 抬頭 | ✅ 一致 | 均為 `{plan_title} 教育訓練測驗成績單`（14pt）＋右上角列印時間 |
| 資訊區 | ✅ 一致 | 姓名/員工編號/部門/訓練計畫/測驗日期/總分/結果均呈現 |
| 成績區 | ✅ 一致 | 通過→綠色、未通過→紅色文字 |
| 簽名區 | ✅ 一致 | 雙欄「考生簽名 / 日期」，位於封面底部 |
| 文案 | ✅ 無自創文案 | 已移除「考試狀態：{att_status}（未完成考試）」，未應考者只顯示封面無額外文字 |

## 4.1 驗收清單（請逐項勾選）
> **狀態（2026-05-01）**：下列第 1、3、4 項已與程式／UAT 對齊完成；清單內 `[ ]` 已改為 `[X]` 供索引一致。

2026/804/30 需修改：
- [X] 1. 部門 individual PDF 與個人查看詳情預覽版型一致
        - 可能我說錯了，或不夠清楚。
        - 我要的PDF的正確的路徑：進入個人成績歷史->按查看詳情->跳出`考試歷程記錄Model`->按查看詳情->跳出`成績詳情Model`->按右上的預覽成績單->跳出`成績單預覽Model`->最後按右上的`列印的button`，然後跳出列印預覽的結果。
        - 這才是我要的就是每個成員的考卷的成績單的PDF。
- [X] 2. individual PDF 不再出現「請至系統查看詳情」文案

補充需求：
- [X] 3. 成績清單列印：
        - 清單的表頭及列表列內的文字垂直置中。
        - 因為清單的列表資列的高列（每一列的高度）不足，導致`出席狀態欄`的日期時間會被下一列擋到了，只有最後一筆看的到。
          - 不要分成上下2列（出席狀態在上，日期時間在下），所以改成左右排成一列，中間空一格來顯示。
- [X] 4. 在「部門績效表現」中，點擊某一部門，展開垂直折疊，秀出該部門的成員成績列表：
        - 修改列表，靠右對齊，就是將整個列表（整個表格）由現在是靠左，改成靠右對齊。

---

**關閉註記（2026-05-01）**：本輪與後續清單列高、出席狀態左右排列、展開區對齊與欄寬（見 `20260501_成員成績列表欄寬調整-2.md`）已驗收完成。
