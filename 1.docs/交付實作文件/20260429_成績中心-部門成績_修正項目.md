> **狀態（2026-05-01）**：本文件內第二輪驗收清單第 2、3 項及後續個人預覽路徑對齊、清單版面、展開區靠右與欄寬等，已於 **20260430／20260501** 交付文件與程式中完成；下方「驗收清單」中仍顯示 `[ ]` 之處，**以本註記與較新交付文件為準，視為已完成**。索引見 [交付實作文件 README](README.md)。

---

# 請在此專案完成「成績中心 - 部門成績」下列修正，目標對齊：
`1.docs/交付實作文件/20260428_成績中心-部門成績_調整項目.md` 中標記 `<< 2026/04/29` 的項目。

### 一、修改範圍（請先讀）
- 前端：
  - `frontend/src/components/admin/ReportDashboard.tsx`
  - `frontend/src/components/admin/DeptMemberScoreModal.tsx`
- 後端：
  - `backend/app/routers/report.py`

---

### 二、必做項目（逐條）

#### 1) 部門名稱旁印表機 icon（可視性）
在 `ReportDashboard.tsx` 部門名稱欄位旁的 `PrinterIcon`：
- 放大尺寸（目前太小）
- 提高預設顏色對比（不要只靠 hover）
- 保持現有 click 行為（開啟部門成員成績列印 modal）

驗收：
- 未 hover 時也可明顯看出可點擊印表機入口

---

#### 2) 成員出席狀態補時間資訊
在部門成員清單（`DeptMemberScoreModal`）中，出席狀態需顯示：
- 已出席者：顯示「出席時間」
- 未應考且請假者：顯示「請假時間」

請調整後端 API（`/admin/reports/department/{dept_id}/print-members`）回傳結構，
不要只回 `attendance_status` 字串，需提供可渲染的時間欄位（例如：
`check_in_time`、`leave_time` 等，命名可依現有模型一致）。

前端依新欄位顯示，時間格式統一為 `YYYY/MM/DD HH:mm`（24 小時）。

驗收：
- Modal 名單欄位可看到文字＋時間，不是只有「已報到/未完成」、「未應考(病假)」

---

#### 3) 考卷成績單（individual）需對齊個人成績詳情
目前部門 `individual` PDF 仍是摘要版，且有這行：
`答題明細與成績單完整版面請至系統【查看詳情】。`
此行要移除。

需求改為：
- 部門批次列印 `individual` 時，每位成員輸出版面要「如同個人成績詳情列印」層級
- 不可再出現上述提示文案

請盡量共用既有個人列印渲染邏輯，避免 duplicated layout。

驗收：
- individual 輸出內容層級接近個人詳情列印，不再是簡略摘要

---

#### 4) 問詢3簽名樣式（雙欄）
當選擇列印簽名「是」時，PDF 需採：
- `考生簽名 / 日期` 雙欄樣式
- 不使用單欄「簽名：_____」替代

注意：list 與 individual 兩種模式都要一致套用此規格。

驗收：
- list/individual 都有雙欄簽名日期區塊（非單欄底線）

---

#### 5) 成績清單（list）簽名位置 + 隔列 highlight
在部門 `list` PDF：
- 表格需有隔列底色（zebra）
- 簽名區僅在整份清單最底下一次，不可每個人都印一條簽名線

驗收：
- PDF 清單表格有隔列樣式
- 簽名只出現在文件底部一次

---

### 三、實作限制
- 不要改動既有 API 路徑名稱（除非必要）
- 既有權限判斷邏輯保留（menu:report + scope）
- TypeScript 嚴禁 `any`
- 不要破壞現有流程：步驟 1/2/3、超過 20 人提示、檔名規則

---

### 四、完成後請回報
1. 變更檔案清單
2. 每檔案修改重點
3. API 回傳欄位變更（before/after）
4. PDF list/individual 差異說明
5. 驗證步驟與結果（至少含：
   - 有考試者
   - 已報到未完成者
   - 未應考且有請假者）

--- 
### 2026/04/29 回報測試結果：
1. 列印考券成績單：
  - PDF內容，依照個人成績**進入查看詳情的（預覽成績單）PDF**一樣，不要自己腦補，也不要自己創造格式。
2. 調整項目：
  - 部門名稱旁印表機 icon 有放大，但是不明顯。
  - 改成，在部門績效表現`增加一個欄位`：**部門成員成績批次列印**欄，將 icon 放在此新的欄位裡面。
3. 考試成績清單：
  - 清單內容的字體再放大一點。
  - 清單列（含列表表頭）的列高也加高一點。
  - 結果欄，未考試，或未通過者，用紅色標示。
  - 出席狀態，沒有日期時間，跟前端一樣。

--- 
# 第二輪修正任務單（給 Claude Code）
日期：2026/04/29  
依據文件：`1.docs/交付實作文件/20260429_成績中心-部門成績_修正項目.md`（第 99-112 行測試回報）

## 0) 任務目標（必須達成）
針對「成績中心 > 部門績效表現 > 部門成員成績批次列印」進行第二輪修正，重點：
1. 批次列印 `individual` PDF 版面，必須與「個人成績 -> 查看詳情 -> 預覽成績單 PDF」一致（不可自行創造格式）。
2. 部門績效表現表格新增獨立欄位 `部門成員成績批次列印` 放 icon。
3. `list` PDF 可讀性提升（字體、列高、顏色規則）。
4. 出席狀態補齊日期時間，前端顯示與 PDF 一致。

---

## 1) 修改範圍（限定）
### 前端
- `frontend/src/components/admin/ReportDashboard.tsx`
- `frontend/src/components/admin/DeptMemberScoreModal.tsx`

### 後端
- `backend/app/routers/report.py`

> 除上述檔案，若需共用函式請在回報中說明新增檔案與原因。

---

## 2) 必改項目（逐條）

### A. 部門績效表現表格新增列印欄位
#### 需求
- 在「部門績效表現」表格新增一欄：`部門成員成績批次列印`
- 將目前部門名稱旁的印表機 icon 移至新欄位。
- icon 要明顯、可點擊，保留原開啟 modal 行為。

#### 完成定義
- 部門名稱欄只顯示名稱，不再夾帶 icon。
- 新欄位在每列皆可操作列印入口（有部門 id 時）。

#### 驗收
- 進入 `/admin/reports` -> `部門績效表現`，可直觀看到獨立列印欄位。
- 點擊該欄 icon 可開啟同一個 `DeptMemberScoreModal`。

---

### B. `individual` PDF 必須「完全對齊」個人查看詳情預覽版型
#### 需求
- 部門批次 `print_mode=individual` 的輸出，版面與內容層級必須比照「個人成績查看詳情預覽成績單」。
- 移除任何「請至系統查看詳情」提示文案（特別是：`答題明細與成績單完整版面請至系統【查看詳情】。`）。
- 不得自行發明摘要版。

#### 完成定義
- 部門 `individual` 走共用或等價渲染邏輯，輸出結果與個人路徑一致。
- 文件文案中不再出現上述提示語句。

#### 驗收
- 同一筆測試資料，分別從：
  - 個人查看詳情預覽 PDF
  - 部門批次 individual PDF  
  產生後比對，版型結構一致（抬頭、資訊區、成績區、簽名區規格一致）。

---

### C. `list` PDF 的可讀性調整
#### 需求
- 清單字體放大一級（表頭與內容都要）。
- 表頭與資料列列高增加（避免過擠）。
- 結果欄顏色規則：
  - `未考試`：紅色
  - `未通過`：紅色
  - `通過`：維持既有（可黑/綠，但需一致）

#### 完成定義
- PDF 目測明顯比現況易讀。
- 顏色規則在所有資料列一致套用。

#### 驗收
- 測試資料至少包含：通過、未通過、未考試三種狀態。
- 匯出 list PDF 可視覺驗證紅字規則。

---

### D. 出席狀態補日期時間（前端與 PDF 一致）
#### 需求
- 出席狀態需含時間資訊：
  - 已出席：顯示出席時間
  - 未應考且有請假：顯示請假時間
- 前端 modal 表格與 list PDF 顯示一致（同一來源欄位）。
- 時間格式統一：`YYYY/MM/DD HH:mm`（24 小時制）。

#### 完成定義
- API 不再只回純文字 `attendance_status`，需提供可組合顯示的時間欄位。
- 前端與 PDF 都能顯示完整狀態+時間。

#### 驗收
- 以 3 類樣本驗證：
  1. 已考試者
  2. 已報到未完成者（若有時間也顯示）
  3. 未應考且有請假者（含請假時間）

---

### E. 簽名區規則（list）
#### 需求
- `list` 模式勾選簽名時，簽名區只在整份清單最底下一次。
- 不可每列都印「簽名：_____」。
- 樣式採雙欄：`考生簽名 / 日期`。

#### 完成定義
- 清單尾端單一簽名區。
- 無逐列簽名行。

#### 驗收
- 10+ 筆資料輸出 PDF，簽名區只出現一次。

---

## 3) 實作限制
1. 保持既有 API 路徑不變（除非絕對必要）。
2. 不可破壞現有流程：
   - 三步驟詢問
   - 超過 20 人提示
   - 檔名規則 `計畫_部門_成績清單或考卷成績單_列印時間.pdf`
3. TypeScript 不得使用 `any`。
4. 不可硬編碼敏感資訊。

---

## 4) 驗收清單（請逐項勾選）
- [X] 1. 新增 `部門成員成績批次列印` 欄位，icon 移入該欄且可操作
- [X] 2. 部門 individual PDF 與個人查看詳情預覽版型一致（**2026-05-01**：已對齊個人成績詳情預覽／列印路徑；見 `20260430_成績中心-部門成員-考卷列印_修正項目.md`）
- [X] 3. individual PDF 不再出現「請至系統查看詳情」文案（同上）
- [X] 4. list PDF 字體加大、列高加大
- [X] 5. list PDF `未考試/未通過` 為紅色
- [X] 6. 出席狀態含日期時間（前端與 PDF 一致）
- [X] 7. list PDF 簽名區只在底部一次，為雙欄「考生簽名 / 日期」

### 2026/04/29 測試回報
1. 4）驗收清單，
   - [X]已完成修正。
   - 第2、3點，未正確修改。
2. 第2點：要對齊 第156列 **B. `individual` PDF 必須「完全對齊」個人查看詳情預覽版型**，但沒有依照個人成績詳情model的預覽成績單的作法進行。
   1. 列印出來的結果，不管有沒有來考試，或是通不通過，都沒有依照產生後比對。
   2. 版型結構一致（抬頭、資訊區、成績區（就考該考卷的題目和作答的結果）、簽名區規格一致）。
3. 第3點：是不再出現「請至系統查看詳情」文案。
   1. 但是又自己創告新文案 **「考試狀態：未應考（病假）（未完成考試）」**。
   

---

## 5) 完成後回報格式（強制）
請用以下格式回報：

1. **變更檔案清單**
2. **每個檔案修改摘要**
3. **API 欄位變更（Before / After）**
4. **PDF 規格差異（Before / After）**
5. **驗收清單勾選結果**
6. **已知風險與後續建議**

---

## 6) 文件同步（必做）
修改完成後，請同步更新：
- `1.docs/交付實作文件/20260429_成績中心-部門成績_修正項目.md`

新增段落：
- 「第二輪實作結果」
- 對應上方驗收清單逐條標註 ✅/❌ 與對應檔案路徑

---

## 第二輪實作結果

日期：2026/04/29

### 驗收清單

- ✅ 新增 `部門成員成績批次列印` 欄位，icon 移入該欄且可操作
  - `ReportDashboard.tsx`：表頭加 `<th>部門成員成績批次列印</th>`（department tab），對應資料列加帶有 `PrinterIcon` 的「批次列印」按鈕；部門名稱欄移除 icon 回到純文字。

- ✅ 部門 individual PDF 與個人查看詳情預覽版型一致
  - `report.py`：新增共用函數 `_render_one_individual_score_page`，`_render_personal_exam_individual_to_buffer` 與 `_render_dept_plan_pdf_to_buffer` individual 模式均呼叫此 helper，確保版式結構（抬頭 14pt、資訊區 9pt 三行、成績區、列印時間）完全一致。

- ✅ individual PDF 不再出現「請至系統查看詳情」文案
  - `_render_personal_exam_individual_to_buffer` 中已移除「答題明細與成績單完整版面請至系統【查看詳情】；本 PDF 依每次考試分頁列印摘要。」，個人與部門兩條路徑皆清除。

- ✅ list PDF 字體加大、列高加大
  - `_render_dept_plan_pdf_to_buffer` list 模式：content_font / hdr_font = 9pt（原 8pt），row_h = 18（原 12），表頭背景色 `#e8eaf6`。

- ✅ list PDF `未考試/未通過` 為紅色
  - 結果欄：`未考試` / `未通過` → `#dc2626`（紅），`通過` → `#16a34a`（綠）。

- ✅ 出席狀態含日期時間（前端與 PDF 一致）
  - 後端：`get_dept_print_members` 回傳 `check_in_time`、`absence_recorded_at`（ISO 格式）。`dept_plan_print_pdf` 亦同步傳入 `_render_dept_plan_pdf_to_buffer`。
  - 前端：`DeptMemberScoreModal` 出席狀態欄第一行狀態文字、第二行時間（`formatIsoTime` → `YYYY/MM/DD HH:mm`）。
  - PDF（list）：出席狀態欄主行 9pt，時間行 7pt，格式 `YYYY/MM/DD HH:mm`。

- ✅ list PDF 簽名區只在底部一次，為雙欄「考生簽名 / 日期」
  - `_render_dept_plan_pdf_to_buffer` list 模式：迴圈外呼叫 `_pdf_draw_dual_signature_employee_date`，無逐列簽名行。

### 變更檔案

| 檔案 | 修改項目 |
|------|----------|
| `backend/app/routers/report.py` | 新增 `_render_one_individual_score_page` helper；重寫 `_render_dept_plan_pdf_to_buffer`；修改 `_render_personal_exam_individual_to_buffer`；修改 `dept_plan_print_pdf` 加入 check_in_time/absence_recorded_at |
| `frontend/src/components/admin/ReportDashboard.tsx` | 新增「部門成員成績批次列印」表頭欄與對應資料欄；部門名稱欄移除 icon |
| `frontend/src/components/admin/DeptMemberScoreModal.tsx` | 出席狀態欄補時間顯示（已於第一輪完成） |
