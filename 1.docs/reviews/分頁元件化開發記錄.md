# 分頁元件化開發記錄

> **日期**：2026-01-25  
> **分支**：`feature/pagination-component`  
> **目標分支**：`feature/ui-ux-pro-max-redesign`

---

## 1. 目的

將人員管理清單使用的分頁功能抽取為通用元件，並在以下頁面中統一使用：
- 單位管理（DepartmentManager）
- 訓練計畫管理（TrainingPlanManager）
- 考卷工坊（ExamStudio）
- 題庫維護（QuestionBankManager）
- 成績中心的「部門績效表現」和「訓練計畫成效」（ReportDashboard）

---

## 2. 分支前檢查

### 2.1 確認當前分支狀態

```bash
git status
```

**預期結果**：工作區乾淨，無未提交的變更

```
位於分支 feature/ui-ux-pro-max-redesign
您的分支與上游分支 'origin/feature/ui-ux-pro-max-redesign' 一致。
沒有要提交的檔案，工作區為乾淨狀態
```

### 2.2 確認最近提交記錄

```bash
git log -3 --oneline
```

**用途**：了解當前分支的最新狀態，確保從正確的基礎開始

---

## 3. 創建功能分支

### 3.1 建立新分支

```bash
git checkout -b feature/pagination-component
```

**輸出**：
```
已切換至新分支「feature/pagination-component」
```

### 3.2 確認分支已切換

```bash
git branch
```

**輸出**：確認 `*` 標記在新分支上

---

## 4. 開發工作

### 4.1 新增檔案

| 檔案路徑 | 說明 |
|---------|------|
| `frontend/src/components/common/Pagination.tsx` | 通用分頁元件 |

### 4.2 修改檔案

| 檔案 | 變更說明 |
|------|---------|
| `UserManager.tsx` | 抽出分頁邏輯，改用 `Pagination` 元件 |
| `DepartmentManager.tsx` | 新增分頁功能 |
| `TrainingPlanManager.tsx` | 新增分頁功能 |
| `ExamStudio.tsx` | 題目清單新增分頁功能 |
| `QuestionBankManager.tsx` | 替換原有分頁為 `Pagination` 元件 |
| `ReportDashboard.tsx` | 部門績效表/訓練計畫成效表新增分頁功能 |

### 4.3 Pagination 元件 API

```typescript
interface PaginationProps {
  /** 當前頁碼（從 1 開始） */
  currentPage: number;
  /** 總頁數 */
  totalPages: number;
  /** 每頁顯示筆數 */
  pageSize: number;
  /** 總筆數 */
  totalItems: number;
  /** 頁碼變更回調 */
  onPageChange: (page: number) => void;
  /** 每頁筆數變更回調 */
  onPageSizeChange: (size: number) => void;
  /** 每頁筆數選項，預設 [5, 10, 20, 50] */
  pageSizeOptions?: number[];
  /** 是否顯示每頁筆數選擇器，預設 true */
  showPageSizeSelector?: boolean;
  /** 是否顯示總筆數，預設 true */
  showTotalItems?: boolean;
  /** 自定義樣式類名 */
  className?: string;
}
```

### 4.4 使用範例

```tsx
import Pagination from '../common/Pagination';

// 在元件中使用
<Pagination
  currentPage={currentPage}
  totalPages={totalPages}
  pageSize={pageSize}
  totalItems={filteredItems.length}
  onPageChange={setCurrentPage}
  onPageSizeChange={(size) => {
    setPageSize(size);
    setCurrentPage(1);
  }}
/>
```

---

## 5. 合併前檢查

### 5.1 確認所有變更已提交

```bash
git status
```

**預期結果**：
```
位於分支 feature/pagination-component
沒有要提交的檔案，工作區為乾淨狀態
```

### 5.2 檢查提交記錄

```bash
git log -3 --oneline
```

**輸出範例**：
```
e8f893e 新建Pagination 分頁元件 API...
4c8e5c1 完成 UI UX Pro Max 設計系統套用 題庫維護頁面
c545417 修改成績單列印，在macOS下列印中文字無法正常顯示問題。
```

### 5.3 檢查變更統計

```bash
git diff --stat feature/ui-ux-pro-max-redesign
```

**用途**：確認即將合併的變更範圍

---

## 6. 執行合併

### 6.1 切換到目標分支

```bash
git checkout feature/ui-ux-pro-max-redesign
```

**輸出**：
```
已切換至分支「feature/ui-ux-pro-max-redesign」
```

### 6.2 合併功能分支

```bash
git merge feature/pagination-component --no-edit
```

**輸出**（Fast-forward 合併）：
```
更新 4c8e5c1..e8f893e
Fast-forward
 .../src/components/admin/DepartmentManager.tsx     |  48 +++++-
 frontend/src/components/admin/ExamStudio.tsx       |  50 ++++++-
 .../src/components/admin/QuestionBankManager.tsx   |  38 ++---
 frontend/src/components/admin/ReportDashboard.tsx  |  38 ++++-
 .../src/components/admin/TrainingPlanManager.tsx   |  49 ++++++-
 frontend/src/components/admin/UserManager.tsx      | 101 ++-----------
 frontend/src/components/common/Pagination.tsx      | 162 +++++++++++++++++++++
 7 files changed, 351 insertions(+), 135 deletions(-)
 create mode 100644 frontend/src/components/common/Pagination.tsx
```

### 6.3 刪除已合併的功能分支

```bash
git branch -d feature/pagination-component
```

**輸出**：
```
已刪除分支 feature/pagination-component（曾為 e8f893e）。
```

---

## 7. 合併後檢查

### 7.1 確認當前分支狀態

```bash
git status
```

**預期結果**：
```
位於分支 feature/ui-ux-pro-max-redesign
您的分支領先 'origin/feature/ui-ux-pro-max-redesign' 共 N 個提交。
沒有要提交的檔案，工作區為乾淨狀態
```

### 7.2 確認合併結果

```bash
git log -5 --oneline
```

**用途**：確認提交歷史正確

### 7.3 推送到遠端

```bash
git push
```

**輸出**：
```
To github.com:wilsontt/2.Edu_TraningSystem.git
   c7877ec..e8f893e  feature/ui-ux-pro-max-redesign -> feature/ui-ux-pro-max-redesign
```

---

## 8. 變更摘要

| 項目 | 數值 |
|------|------|
| 新增檔案 | 1 |
| 修改檔案 | 6 |
| 新增行數 | +351 |
| 刪除行數 | -135 |
| 淨增行數 | +216 |

### 檔案變更明細

| 檔案 | 新增 | 刪除 | 說明 |
|------|------|------|------|
| `Pagination.tsx` | +162 | 0 | 新增通用分頁元件 |
| `DepartmentManager.tsx` | +48 | 0 | 新增分頁功能 |
| `ExamStudio.tsx` | +50 | 0 | 新增題目分頁 |
| `QuestionBankManager.tsx` | +19 | -19 | 改用分頁元件 |
| `ReportDashboard.tsx` | +38 | 0 | 新增分頁功能 |
| `TrainingPlanManager.tsx` | +49 | 0 | 新增分頁功能 |
| `UserManager.tsx` | +15 | -116 | 抽出分頁邏輯 |

---

## 9. 功能驗證清單

- [ ] 人員管理分頁功能正常
- [ ] 單位管理分頁功能正常
- [ ] 訓練計畫管理分頁功能正常
- [ ] 考卷工坊題目清單分頁功能正常
- [ ] 題庫維護分頁功能正常
- [ ] 成績中心部門績效表分頁功能正常
- [ ] 成績中心訓練計畫成效表分頁功能正常
- [ ] 每頁筆數切換功能正常
- [ ] 頁碼輸入跳頁功能正常

---

## 10. 注意事項

1. **分頁元件位置**：`frontend/src/components/common/Pagination.tsx`
2. **預設每頁筆數選項**：`[5, 10, 20, 50]`
3. **設計系統一致性**：元件已套用 UI UX Pro Max 設計系統（Primary: #4F46E5 indigo）
4. **後端分頁**：`QuestionBankManager` 使用後端 API 分頁，其他頁面使用前端分頁
