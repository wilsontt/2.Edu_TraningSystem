# 業務資料清空：訓練計畫／報到／成績／考卷（待刪對齊）

| 項目 | 內容 |
|------|------|
| 日期 | 2026-07-22 |
| 狀態 | **已執行清空（2026-07-22）** |
| 資料庫 | `data/education_training.db`（盤點時間：2026-07-22） |
| 前身檔名 | `20260722_測試帳號清理_待刪紀錄對齊.md`（已改名；測試帳清理見附錄 A，**已於同日執行完畢**） |
| 目的 | 清空訓練計畫、報到總覽、成績中心、考卷工坊（計畫題）相關業務資料；**歷史題庫與教材庫保留** |

---

## 1. 目的

在保留人員／組織主檔、歷史題庫、教材庫的前提下，將「訓練計畫 → 報到 → 考試成績 → 考卷工坊計畫題」相關紀錄**全部刪除、不留一筆**，供後續以乾淨狀態重建業務資料。本文件僅供對齊；確認後另下指令執行。

---

## 2. 範圍

### 2.1 納入清空（模組對照）

| 模組 | 處置 |
|------|------|
| 訓練計畫 | 計畫本體、受課單位／個人全刪 |
| 報到總覽 | 報到、批次、未到原因、報到事件全刪 |
| 成績中心 | 考試紀錄、明細、歷程、重考授權全刪 |
| 考卷工坊 | 掛在計畫上的題目 `questions` 全刪 |
| QR／登入碼 | `login_tokens` 一併刪（報到／開考常用） |
| 教材↔計畫綁定 | 只刪／清空綁定，**不刪**教材本體 |

### 2.2 明確保留

| 表／對象 | 盤點筆數 | 說明 |
|----------|---------|------|
| `question_bank` | 42 | 歷史題庫 |
| `teaching_materials` | 32 | 教材主檔（僅清 `plan_id`） |
| `teaching_material_sets` | 28 | 教材套組 |
| `teaching_material_files` | 35 | 套組檔案 |
| `material_types`／`material_file_formats` | 8／12 | 教材類型與允許格式 |
| `users` | 107 | 人員 |
| `departments`／`roles`／職務／功能權限 | — | 組織與 RBAC |
| `main_categories`／`sub_categories` | 6／21 | 訓練分類主檔（計畫刪後仍可再建） |
| `file_transfer_audit_logs` | 43 | 教材 NAS 傳輸稽核 |
| `admin_login_otps`／`backup_*` | — | 認證備援／備份維運 |
| `role_department_scope_*` | — | 角色資料範圍設定 |

### 2.3 不在本次範圍

- 不刪真實離職員工帳號（`_停用` 內正式人員）。
- 不刪 NAS 實體檔（僅 DB 關聯；教材列保留）。

---

## 3. 權責

| 角色 | 權責 |
|------|------|
| 需求方 | 確認範圍、保留項、可執行清空 |
| 資料維運／開發 | 備份後依 §5 順序執行；更新本文件執行紀錄 |
| 本文件 | 對齊清單；勾選完成前**不得**當成已清空證明 |

---

## 4. 名詞解釋

| 名詞 | 說明 |
|------|------|
| 計畫題 `questions` | 考卷工坊寫入各訓練計畫的題目；與 `question_bank` 歷史題庫分開 |
| 歷史題庫 | `question_bank`，本次**保留** |
| 教材庫 | 教材／套組／檔案主檔，本次**保留**；僅解除與計畫的綁定 |
| 成績中心資料 | `exam_records` 及其子表／重考授權 |
| 報到總覽資料 | `attendance_*` 全系列 |

---

## 5. 作業內容

### 5.1 待刪／待清總覽（盤點當下）

| 順序 | 動作 | 表 | 筆數 | 備註 |
|------|------|-----|------|------|
| 1 | DELETE | `exam_retake_authorizations` | **4** | 指到 exam_history |
| 2 | DELETE | `exam_details` | **677** | |
| 3 | DELETE | `exam_history` | **106** | |
| 4 | DELETE | `exam_records` | **53** | 成績中心主表 |
| 5 | DELETE | `attendance_batch_absence_reasons` | **12** | |
| 6 | DELETE | `attendance_absence_reasons` | **51** | |
| 7 | DELETE | `attendance_checkin_events` | **90** | |
| 8 | DELETE | `attendance_checkin_batch_plans` | **40** | |
| 9 | DELETE | `attendance_checkin_batches` | **19** | |
| 10 | DELETE | `attendance_records` | **76** | 傳統報到 |
| 11 | DELETE | `login_tokens` | **20** | QR／一次性登入碼 |
| 12 | DELETE | `questions` | **130** | 考卷工坊計畫題 |
| 13 | DELETE | `plan_target_users` | **10** | 個人受課 |
| 14 | DELETE | `plan_target_departments` | **106** | 單位受課 |
| 15 | DELETE | `teaching_material_set_plans` | **13** | 套組↔計畫綁定 |
| 16 | UPDATE | `teaching_materials.plan_id → NULL` | **7** | **不刪**教材列 |
| 17 | DELETE | `training_plans` | **10** | 最後刪計畫本體 |

**約計**：DELETE 列數約 **1,417**；另 UPDATE 教材綁定 7 筆。

### 5.2 將刪除的訓練計畫（10）

| id | title | training_date | year | is_archived |
|----|-------|---------------|------|-------------|
| 1 | 2026年度新人培訓 | 2026-01-05 | 2026 | 0 |
| 2 | 2026年度教育訓練-消防演練 | 2026-01-06 | 2026 | 0 |
| 3 | 2025 資訊部 AI 運用訓練 | 2025-10-28 | 2025 | 0 |
| 4 | 2026年 資訊安全 | 2026-01-05 | 2026 | 0 |
| 5 | T5 Verification Exam | 2026-01-06 | 2026 | 1 |
| 6 | 2026 Q1 資安意識測驗 | 2026-01-05 | 2026 | 0 |
| 7 | 2026年度教育訓練 | 2026-01-07 | 2026 | 0 |
| 8 | 2026老人計劃 | 2026-01-09 | 2026 | 0 |
| 9 | Docker | 2026-01-27 | 2026 | 0 |
| 10 | AI天然吔～尚好 | 2026-06-22 | 2026 | 0 |

### 5.3 教材綁定（清關聯、保留本體）

**`teaching_materials`：`plan_id` 改 NULL（7 筆）**

| id | title | 原 plan_id |
|----|-------|------------|
| 14 | education_training_db_結構分析 | 10 |
| 15 | MIGRATION_GUIDE | 10 |
| 16 | 專案架構分析 | 10 |
| 17 | 1.成績中心功能計劃 | 10 |
| 18 | 2.成績中心開發任務 | 10 |
| 19 | 3.測試報告與已知問題 | 10 |
| 28 | 說明檔 | 10 |

**`teaching_material_set_plans`：整表刪除（13 筆）**

| set_id | plan_id |
|--------|---------|
| 10 | 10 |
| 11 | 10 |
| 12 | 10 |
| 13 | 4 |
| 13 | 10 |
| 14 | 10 |
| 21 | 10 |
| 24 | 10 |
| 25 | 10 |
| 26 | 7 |
| 27 | 7 |
| 28 | 1 |
| 28 | 10 |

### 5.4 建議 SQL 骨架（對齊確認後再執行）

> **Docker 環境（ds1／corwndocker）**：勿只在本機跑此段。請依  
> **[20260722_Docker環境_業務資料清空操作SOP.md](20260722_Docker環境_業務資料清空操作SOP.md)**  
> 先備份 `${DATA_ROOT}/training/education_training.db`，再於 `training-backend` 容器內以 Python（或 sqlite3）執行下列等價 SQL。兩台主機 volume 分開，須**各執行一次**。

```sql
-- 執行前請先備份 data/education_training.db
-- （Docker：見 20260722_Docker環境_業務資料清空操作SOP.md）
BEGIN;


DELETE FROM exam_retake_authorizations;
DELETE FROM exam_details;
DELETE FROM exam_history;
DELETE FROM exam_records;

DELETE FROM attendance_batch_absence_reasons;
DELETE FROM attendance_absence_reasons;
DELETE FROM attendance_checkin_events;
DELETE FROM attendance_checkin_batch_plans;
DELETE FROM attendance_checkin_batches;
DELETE FROM attendance_records;

DELETE FROM login_tokens;

DELETE FROM questions;

DELETE FROM plan_target_users;
DELETE FROM plan_target_departments;

DELETE FROM teaching_material_set_plans;
UPDATE teaching_materials SET plan_id = NULL WHERE plan_id IS NOT NULL;

DELETE FROM training_plans;

-- 驗證（應為 0；教材／題庫／users 應仍有資料）
-- SELECT COUNT(*) FROM training_plans;
-- SELECT COUNT(*) FROM exam_records;
-- SELECT COUNT(*) FROM attendance_records;
-- SELECT COUNT(*) FROM questions;
-- SELECT COUNT(*) FROM question_bank;          -- 應仍 > 0
-- SELECT COUNT(*) FROM teaching_materials;    -- 應仍 > 0
-- SELECT COUNT(*) FROM teaching_materials WHERE plan_id IS NOT NULL; -- 應為 0

COMMIT;
```

> 若 SQLite 外鍵約束已開啟且順序衝突，依錯誤訊息調整；上表順序已依子→父設計。

### 5.5 對齊勾選

- [x] 10 筆訓練計畫可全部刪除
- [x] 成績中心／報到相關表可清空（不留一筆）
- [x] 考卷工坊僅刪 `questions`；`question_bank` 保留
- [x] 教材庫本體保留；僅清綁定（set_plans 刪、materials.plan_id 清空）
- [x] `login_tokens` 一併刪除
- [x] 分類主檔、人員／部門／角色、NAS 稽核、備份設定**保留**
- [x] 已備份 DB 後再執行
- [x] 執行後補「執行紀錄」並勾選驗證

---

## 6. 參考文件

- [reviews/README.md](README.md)
- [20260722_Docker環境_業務資料清空操作SOP.md](20260722_Docker環境_業務資料清空操作SOP.md) — ds1／corwndocker 容器內清空步驟
- [系統測試／倉儲人員_20260717.xlsx](../../系統測試/倉儲人員_20260717.xlsx)
- [資料庫結構分析](../../00-專案總覽/資料庫結構分析/education_training_db_結構分析.md)
- [生產部署指南](../../00-專案總覽/生產部署指南.md)

---

## 7. 使用表單（欄位說明）

無紙本表單；以 §5.5 勾選為準。

| 識別 | 說明 |
|------|------|
| `training_plans.id` | 計畫主鍵；刪除前須先清所有 FK 子表 |
| `questions.plan_id` | 計畫題；本次全刪 |
| `question_bank` | 歷史題庫；本次不刪 |
| `teaching_materials.plan_id` | 舊單檔教材綁計畫；改 NULL |
| `teaching_material_set_plans` | 套組綁計畫；整表刪 |

---

## 執行紀錄（業務清空）

| 項目 | 內容 |
|------|------|
| 執行日期 | 2026-07-22 |
| 執行人 | Cursor Agent（依使用者指示「依文件執行清空」） |
| 備份檔名 | `data/education_training.db.bak-20260722-before-business-wipe` |
| 結果 | 成功。待刪表計數皆 **0**；`question_bank=42`、`teaching_materials=32`、`teaching_material_sets=28`、`teaching_material_files=35`、`users=107`、分類 6／21 均保留；教材 `plan_id` 非空 = 0。 |

---

## 附錄 A：測試帳號清理（已完成，2026-07-22）

前身文件主題。已刪 11 個 `_停用` 異常姓名測試帳及其考試／報到／個人受課關聯。

| 項目 | 內容 |
|------|------|
| 狀態 | **已執行刪除** |
| 備份 | `data/education_training.db.bak-20260722-before-delete-test-users` |
| 刪除對象 | `100060`、`900001`～`900003`、`900005`～`900010`、`E100001` |
| 結果 | 相關 `users`／`exam_*`／`attendance_records`／`plan_target_users` 計數為 0；真實離職 6 人仍留在 `_停用` |

**當時刪除順序**：`exam_details` → `exam_history` → `exam_records` → `attendance_records` → `plan_target_users` → `users`。
