# 教材套組：選檔檔名顯示與上傳超限錯誤提示

**日期**：2026-07-13  
**狀態**：✅ 已完成（驗收 2026-07-14）  
**規格脈絡**：[教材上傳列管與教材庫 PLAN](../plans/已完成/20260617_教材上傳列管與教材庫_PLAN.md) §5.12（三層 min 單檔上限）

---

## 1. 目的

1. 編輯套組（訓練計畫／教材庫）選檔後清楚列出檔名與大小，對齊建立套組體驗並提高可讀性。  
2. 上傳失敗時明確標示觸發原因（教材類型／檔案格式／系統硬上限／單次批次總量），**禁止**僅因 HTTP 200 而顯示「傳輸成功」。  
3. 「儲存」一併寫入中繼資料與已選未上傳檔案；不可在尚有選檔時僅回報中繼資料成功。  
4. 編輯時移除套組內檔案須先經應用內確認窗，確定後才軟刪。

---

## 2. 範圍

| 項目 | 說明 |
|------|------|
| 前端 | `SelectedFilesList`、`MaterialSetEditPanel`、`MaterialSetUploadPanel`、`FileTransferModal` |
| 後端 | `_file_size_limit_exceeded_message`、`_batch_upload_total_exceeded_message`；`create_set` / `add_set_files` |

---

## 3. 行為定案

- 有效單檔上限仍為 `min(類型, 格式, 系統硬上限)`。  
- `POST .../sets/{id}/files` 若全部進 `failed`：前端呼叫 `endTransferError`，不呼叫 `endTransferSuccess`。  
- 錯誤文案需含「觸發」層級與各層參考上限。  
- 補充上傳併入「儲存」：有已選檔時會先上傳再寫中繼資料；成功訊息需反映實際上傳數量。  
- 移除套組內檔案：應用內確認窗（含檔名）→ 確定後才呼叫 `DELETE .../files/{id}`。

---

## 4. 驗收

> **驗收確認（2026-07-14）**：產品實測無問題，下列項目勾選結案。

- [x] 教材庫編輯套組選檔後可見檔名＋大小（`text-sm`、深色字）。  
- [x] 訓練計畫建立套組選檔列表同樣清楚。  
- [x] 「簡報教材」套組上傳超過 20MB 之 mp4：傳輸視窗顯示失敗與類型上限說明，列表／NAS 無新檔。  
- [x] 建立套組單檔或總量超限：同樣明確失敗原因。  
- [x] 選檔後直接按儲存：會上傳該檔並寫入中繼資料；成功訊息含上傳數量；列表可見新檔。  
- [x] 未選檔只改標題再儲存：僅更新中繼資料並關閉。  
- [x] 編輯移除檔案先跳出確認窗，取消則不刪；確定後列表減少。

---

## 5. 參考文件

- [教材 PLAN](../plans/已完成/20260617_教材上傳列管與教材庫_PLAN.md)
- [交付實作文件 README](./README.md)
