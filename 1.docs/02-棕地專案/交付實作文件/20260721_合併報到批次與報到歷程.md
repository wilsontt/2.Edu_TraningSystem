# 合併報到批次／報到歷程／報到總覽 QR 權限

**日期**：2026-07-21  
**狀態**：✅ 已實作（分支 `cursor/batch-checkin-history-3b7d`）；人工驗收待勾  
**PLAN**：[../plans/20260720_合併報到批次與報到歷程_PLAN.md](../plans/20260720_合併報到批次與報到歷程_PLAN.md)  
**TASKS**：[../tasks/20260720_合併報到批次與報到歷程_TASKS.md](../tasks/20260720_合併報到批次與報到歷程_TASKS.md)

---

## 1. 目的

1. 依序多場訓練：報到總覽「多場訓練合併報到」→ 一組 QR 寫入多筆 `attendance_records`。  
2. append-only 報到歷程（`attendance_checkin_events`）；`checkin_time` 永不覆蓋。  
3. 批次可關閉／重開。  
4. 報到總覽單計畫 QR **不綁 Owner**（有 `menu:attendance-overview` 即可）。

---

## 2. 部署

```bash
cp data/education_training.db data/education_training.db.bak-$(date +%Y%m%d)
cd backend
.venv/bin/python3 migrations/add_attendance_checkin_batch_and_events.py
```

ds1：

```bash
docker compose exec training-backend python migrations/add_attendance_checkin_batch_and_events.py
```

---

## 3. 落點

| 層 | 路徑 |
|----|------|
| 遷移 | `backend/migrations/add_attendance_checkin_batch_and_events.py` |
| 後端 | `models.py`、`schemas.py`、`services/attendance_checkin.py`、`training.py`、`exam_center.py` |
| 前端 | `AttendanceOverviewPage.tsx`、`CheckInPage.tsx` |
| 測試 | `tests/test_attendance_batch_checkin.py` |

---

## 4. 驗收（人工）

- [ ] 報到總覽「多場訓練合併報到」：場次日區間 OR 關鍵字可列出計畫；分次勾選不清空；跨開始日 ≥2 場可出大 Modal QR  
- [ ] 掃碼後各場應到人數增加；關閉後掃碼失敗；可重開  
- [ ] 非開課單位（有報到總覽權限）可「顯示 QRcode」  
- [ ] 統計 Modal「歷程」可見 events  
