> **狀態（2026-05-01）**：本單所列欄寬與 wrapper 策略已併入 `20260501_成員成績列表欄寬調整-2.md` 與 `ReportDashboard.tsx` 實作；下文保留為規格追溯。

---

請再調整「部門展開 -> 成員成績列表」欄位寬度，並改成可調常數（清楚註解），方便後續自行維護。

## 欄寬新規格（字元寬度基準）
- 姓名：15 字
- 計劃：30 字
- 分數：10 字
- 是否通過：15 字

## 實作要求
1. 在 `ReportDashboard.tsx` 內建立可調常數（例如 `MEMBER_TABLE_COL_WIDTH_CH`），集中管理欄寬。
2. 常數旁加註解，說明「單位為字元寬度（ch）」、「可直接改數字調整欄寬」。
3. 表格套用 `table-fixed`，欄位用常數映射寬度，不要再用 `pr-[xxpx]` 這種硬調。
4. 儲存格維持不換行：
   - `whitespace-nowrap`
   - `overflow-hidden`
   - `text-ellipsis`
5. 版面目標：
   - 盡量利用可用寬度，避免右側留下大塊無效空白
   - 不讓表格內容貼到右邊邊界而壓縮可讀性
   - 請把 max-w 限制從展開外層移除，改為只作用在「成員成績列表」專屬 wrapper；外層維持 w-full + justify-end，表格 wrapper 使用 ml-auto + 合理 min/max width，確保水平空間吃滿且不影響下方圖表與 Top10。
6. 本次先只改這 4 欄（姓名、計劃、分數、是否通過），其餘欄位維持現況。

## 註解範例（請放在常數上方）
- 「成員成績列表欄寬設定（單位：ch）。若要調整欄寬，直接修改下列數值。」

## 回報要求
1. 貼出常數區塊（包含註解）
2. 說明每個欄位實際套用位置
3. 說明如何改一個欄位寬度（給維護者看的步驟）