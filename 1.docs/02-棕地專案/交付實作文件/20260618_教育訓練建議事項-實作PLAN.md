# 1. 總提示詞（Session 開場，貼一次即可）

## 任務：教育訓練系統 — 20260618 建議事項實作

### 你的角色
你是本專案的全端工程師。嚴格依 PLAN 實作，不得自行擴充規格。

### 必讀文件（依序）
1. 專案根目錄 `CLAUDE.md`（啟動、架構、規範）
2. `1.docs/02-棕地專案/plans/已完成/20260618_教育訓練_建議事項_實作_PLAN.md`（Wave 分工，**本次執行準據**）
3. `1.docs/02-棕地專案/plans/已完成/20260618_教育訓練_建議事項_PLAN.md`（規格定案）
4. 各 Wave 交叉引用：
   - NAS：`1.docs/02-棕地專案/plans/已完成/20260612_系統備援_NAS儲存與排程備份_PLAN.md`
   - 教材：`1.docs/02-棕地專案/plans/已完成/20260617_教材上傳列管與教材庫_PLAN.md`
5. **不實作** AD 整合（`20260612_AD整合_系統管理者登入_PLAN.md` 另案）

### 實作原則
- 語言：繁體中文（註解、溝通、commit 說明）
- 一次只完成**一個 Wave**；完成後停下來報告，等我確認再進下一 Wave
- 最小 diff：不順手重構無關程式
- TypeScript 禁 `any`；後端 async/await
- 資料庫變更：先備份說明，遵循 `backend/migrations/` 模式
- NAS 密碼、SMB 帳密：**不得**入版控、不得寫入 DB 明文
- 每 Wave 結束：更新實作 PLAN 檢查清單、執行驗證（lint/build/手動步驟）

### 環境
- NAS SMB **已可連線**；Wave 2 直接用 `smbprotocol`
- 前端 base：`/training/`；API 代理 `/training/api` → port 8000

### 分支
請在 `feature/20260618-suggestions` 上工作（若不存在則建立）。

### 本次 Wave
（下方填入 Wave 0 / 1A / 1B / 2 / 3 / 4 / 5 / 6）

# 2. Wave 0 提示詞（P0，建議先做）
請實作 **Wave 0：考試中心按鈕失效**。

### 問題
進入考試中心 → 跳到其他頁 → 回到考試中心，「立即報到／開始考試」不能按，需 F5 才恢復。

### 修改範圍（僅前端）
- `frontend/src/components/exam/ExamDashboard.tsx`
- `frontend/src/components/exam/CheckInButton.tsx`

### 實作要求
1. `useLocation()`：pathname 回到 `/` 時 refetch 考試列表
2. 離開頁面 cleanup：reset `showNotCheckedInModal`、`pendingStartExamId`、`quickCheckInLoading`
3. `CheckInButton` 在父層返回時 refetch（優先於 `key={location.key}` remount）

### 驗收
- 考試中心 → 成績中心（或其他頁）→ 返回考試中心 → 按鈕可正常點擊，無需 F5
- 執行 `npm run lint`（專案根目錄）

### 交付
- 說明根因與修改點
- 勾選實作 PLAN Wave 0 檢查清單
- **不要**開始 Wave 1

# 3. Wave 1A 提示詞（QR 方案 A）
請實作 **Wave 1A：QR Code 方案 A**。

### 定案
- QR 圖內容 = `http://{server}/training/login`（短 URL，**不含 UUID**）
- 前端**不顯示** QR 有效時間
- 廢止對外 token QR 流程（`/auth/login/qrcode/{token}` 改 redirect `/login` 或 deprecated）

### 修改檔案
- `backend/app/routers/qrcode.py`
- `frontend/src/components/admin/QRCodeManager.tsx`
- `frontend/src/components/QRCodeLoginPage.tsx`
- 視需要調整 `frontend/src/App.tsx` 路由

### 驗收
- Admin 產生 QR → 掃碼進入登入頁（員編 + 驗證碼）
- QR 圖 URL 無長 UUID
- 管理頁無 expires_at 顯示

### 注意
- 不要實作 AD 登入
- Wave 0 應已完成；若未做請先告知

# 4. Wave 1B 提示詞（受課對象 UX）
請實作 **Wave 1B：訓練計畫受課對象 UX**。

### 定案（建議事項 PLAN §5.7）
1. 個人授課對象清單預設為空
2. 勾選授課單位 → 個人清單**只顯示**該單位人員
3. 「新增個人對象」按鈕 → 可跨單位加人
4. 額外新增的個人顯示在個人卡片（badge 區分「來自單位」vs「額外新增」）
5. **移除**勾選單位時自動寫入全部 `target_user_ids` 的邏輯

### 修改檔案
- `frontend/src/components/admin/TrainingPlanManager.tsx`（主要）
- `backend/app/routers/training.py`（若儲存語意需對齊）
- `backend/app/routers/exam_center.py`（檢查應考名單是否仍正確）

### 驗收
- 新建／編輯計畫：UI 行為符合上述 4 點
- 儲存後重新開啟，名單與 UI 一致
- 受課對象在考試中心應考名單正確

### 注意
- 不要改無關的訓練計畫欄位
- 完成後說明 `target_departments` 與 `target_user_ids` 的儲存策略


# 5. Wave 2 提示詞（NAS 基礎設施，最大後端波次之一）
請實作 **Wave 2：NAS 儲存層 + Audit + 考卷遷移**。

### 準據

- `1.docs/02-棕地專案/plans/已完成/20260612_系統備援_NAS儲存與排程備份_PLAN.md` §5.3
- `1.docs/02-棕地專案/plans/已完成/20260618_教育訓練_建議事項_PLAN.md` §5.2（考卷僅 Audit）

### 要做的事
#### 2.1 設定
- `backend/app/config.py`（或等同）：`SMB_SERVER`、`SMB_SHARE`、`MATERIALS_ROOT`、`BACKUP_ROOT`、`EXAM_SMB_USERNAME`、`EXAM_SMB_PASSWORD`
- `requirements.txt` 加 `smbprotocol`
- `.env.example` 補變數（不含真實密碼）

#### 2.2 `backend/app/services/storage.py`
- `connect(mode, credentials) → save/open/list/delete → disconnect()`
- 三模式：`interactive` | `service` | `backup`
- **禁止**長期 SMB 連線；每次操作結束 disconnect
- 路徑：`{year}/{plan_id}/exams/`、`teaching/`

#### 2.3 Audit
- migration：`file_transfer_audit_logs`（欄位見建議事項 PLAN §7.1）
- `backend/app/services/audit_log.py`

#### 2.4 考卷遷移
- 改寫 `backend/app/routers/exam.py`：移除本地 `Path("data/materials")`，改用 **service** 模式
- 考卷上傳**不要求**使用者 NAS 登入
- 每次傳輸寫 audit（`nas_username='service'`，含 `emp_id` + IP）
- NAS 不可達 → HTTP 503 + 明確訊息

### 驗收
- 考卷 TXT 上傳成功寫入 NAS `exams/`
- `file_transfer_audit_logs` 有記錄
- 單元測試可 mock storage；整合測試需 `.env` SMB 設定

### 不要做的
- 教材庫 API（Wave 3）
- 排程備份 UI（Wave 4）
- 使用者 NAS 登入 UI（Wave 3）


# 6. Wave 3 提示詞（教材庫，範圍最大）
請實作 **Wave 3：教材庫 + 傳輸 UI**。
### 前置條件

Wave 2 已完成（`storage.py` interactive 模式可用）。

### 準據
`1.docs/02-棕地專案/plans/已完成/20260617_教材上傳列管與教材庫_PLAN.md`（全文，尤其 §5.4a、§5.9、§5.11）

### 後端
- migration：`material_types`、`teaching_materials`
- `models.py`、`schemas.py`
- 新增 `backend/app/routers/teaching_materials.py`，註冊 `main.py`
- 端點：`/nas-session/verify`、`/upload`、`/download`、`/batch-download`、conflict-check、列表、軟刪、material-types CRUD
- 教材上傳／下載：**每次**須 NAS 登入（interactive）；傳完 audit + disconnect
- `nas_session_token` 建議 10 分鐘，密碼不存 DB

### 前端
- 新增 `NasLoginModal.tsx`、`FileTransferModal.tsx`（進度 %、AbortController、beforeunload、僅 X／取消可關）
- 訓練計畫編輯頁教材區
- 新增 `TeachingMaterialLibrary.tsx`（教材庫）

### MVP 策略（若時間緊）
可先交付：單檔上傳／下載 + 列表；批次 ZIP 第二 PR（需在回報中說明）

### 驗收
教材 PLAN §5.11 案例；至少手動測：上傳需 NAS 登入、取消傳輸、audit 有記錄

### 驗證
- `npm run lint`、`npm run build`
- 後端 API 可用 `/docs` 抽測


# 7. Wave 4 / 5 / 6 提示詞（精簡版）

## Wave 4 — 排程備份

實作 Wave 4：排程備份（NAS PLAN §5.5～5.6）。
- `backup_schedule_config`（含加密 `backup_nas_*`）、`backup_records`
- `backend/app/services/backup_service.py`：SQLite backup + materials 快照 + ZIP + rotation
- APScheduler 於 main.py lifespan
- 前端：排程設定頁 + 備份紀錄（系統管理）
- backup 模式 SMB；job 結束 disconnect
- Wave 2 storage 為前置

## Wave 5 — 行動 UI

實作 Wave 5：行動 UI（僅 layout，不動 API／商業邏輯）。
- 先改：ExamDashboard.tsx、成績中心相關頁（PersonalScorePage 等）
- Tailwind responsive、safe-area、觸控目標 ≥44px
- 完成後在 `.cursor/skills/` 或專案約定位置新增 skill：`mobile-responsive-training-ui.md`

## Wave 6 — security-review


## Wave 0～5 已完成。請執行 security-review：
- 檢查 NAS 密碼傳遞、audit 完整性、QR 殘留 token 端點、受課對象權限
- 產出：`1.docs/02-棕地專案/reviews/20260618_建議事項_security-review.md`
- 列出發現項與修復建議（可選：直接修復 Critical/High）


# 3. 每 Wave 完成後請 Claude Code 回報的格式
可貼在每次 Wave 提示詞末尾：


### 完成後請回報
1. 變更檔案清單
2. 根因／設計決策（1～3 句）
3. 驗證指令與結果（lint/build/手動步驟）
4. 實作 PLAN 檢查清單勾選狀態
5. 已知限制或待 IT 配合事項
6. **停止**，等我確認後再進下一 Wave


