# 報到／訓練計畫／教材／題庫 新增需求 — 實作任務清單

- **需求來源／PLAN**：[../plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md](../plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md)
- **對應實作計畫**：`/opt/cursor/artifacts/plans/20260717_實作任務_a6fbe12b.plan.md`
- **狀態**：程式實作完成（分支 `cursor/checkin-owner-material-3b7d`）；T4-2～T4-8 待人工瀏覽器驗收
- **建議執行順序**：Phase 2 → Phase 1 → Phase 3 → Phase 4（報到與 Owner 可並行；T3-2 依賴 T2-7）

---

## 0. 已與使用者確認的範圍決策

- [x] 報到 QRcode 產生功能**放在「訓練計畫管理」操作欄**（重用既有 API）；系統管理「QRcode 管理」維持純登入 QR。報到總覽統計 Modal 亦可顯示 QR（棕地延伸）。
- [x] 教材套組編輯鎖定**只做前端 UI 互斥**，不做後端跨使用者鎖。
- [x] Owner 例外角色為**超管／系統管理**（`is_management_role`）。
- [x] `dept_id` nullable、NULL＝不受 owner 限制。
- [x] **不新增 `checkin_type`**：同一組 QR＋單一 `attendance_records`。
- [x] 當天請假／遲到：上課未到場 → 無報到紀錄；考試前再產／再掃同一 QR 可補報到；未到原因可填，**以實際報到紀錄為準**。
- [x] 已報到者進入考試中心略過再報到（沿用 `is_checked_in`）。
- [x] 檔案檢視補刪除；Owner＋超管可刪（軟刪）。
- [x] Owner 套用訓練計畫、QuestionBank、TeachingMaterialSet／File；後續擴充寫入權與考卷工坊考題（見交付文件）。

---

## 一、報到管理：上課前／考試時 一體化（Phase 1）

### 後端
- [x] **T1-1** 確認 `POST /training/plans/{plan_id}/checkin-qrcode/generate` 可重用；僅前端接入（含 Owner 檢查於訓練計畫入口）。
- [x] **T1-1b** `attendance/status`（及 checkin）回傳 `question_count`／`has_exam`，供 CheckInPage 判斷是否導向考試。
- [x] **T1-1c** checkin 冪等；`UNIQUE(emp_id, plan_id)` migration。
- [x] **T1-1d** status／checkin 回傳 `plan_title`。

### 前端
- [x] **T1-2** `TrainingPlanManager.tsx`：操作欄「產生 QRcode」＋ Modal（QR＋複製連結；文案：上課前／考試前同一 QR）。
- [x] **T1-2b** `AttendanceOverviewPage.tsx`：統計 Modal「顯示 QRcode」（延伸）。
- [x] **T1-3** `App.tsx`：`RedirectToLoginWithReturnTo`（未登入帶 `returnTo`，白名單 `/checkin`）。
- [x] **T1-3b** `App.tsx`：`LoginReturnRedirect`（登入後仍尊重 `returnTo`）。
- [x] **T1-4** `LoginPage.tsx`：登入成功讀取白名單 `returnTo`。
- [x] **T1-5** `CheckInPage.tsx`：已登入且未報到 → 自動 checkin；失敗保留手動重試。
- [x] **T1-6** 報到成功後：`question_count > 0` 才顯示「開始考試」；無考題僅「返回首頁／考試中心」。

---

## 二、擁有權（開課單位＝Owner）（Phase 2）

### 後端 — 資料庫與 Model
- [x] **T2-1** `migrations/add_owner_dept_fields.py`。
- [x] **T2-2** `models.py`：`QuestionBank`、`TeachingMaterialSet` 加 `dept_id`。

### 後端 — Owner 判斷邏輯
- [x] **T2-3** `access_scope.can_delete_owned_resource`（別名 `can_modify_owned_resource`：寫入權含編輯／刪除／封存）。

### 後端 — 套用點
- [x] **T2-4** `training.py`：刪除／PUT／封存／產生 QR 加 owner 檢查。
- [x] **T2-5** `exam.py`：上傳／preview 匯入寫入 `QuestionBank` 時帶 `dept_id=plan.dept_id`。
- [x] **T2-6** `question_bank.py`：list `dept_id` 篩選；delete／bulk-delete owner 403。
- [x] **T2-7** `teaching_material_sets.py`：create 必填 `dept_id`；update／list／delete_set／remove_set_file owner。
- [x] **T2-8** `schemas.py`：`dept_id`／`dept_name`。

### 前端
- [x] **T2-9** `QuestionBankManager`：開課單位欄＋搜尋；刪除 disable。
- [x] **T2-10** `MaterialSetUploadPanel`／`PlanMaterialsSection`：開課單位必填／帶入鎖定。
- [x] **T2-11** `MaterialSetEditPanel`：開課單位欄。
- [x] **T2-12** `user` prop 傳遞鏈（App → ExamStudio → TeachingMaterialLibrary）。
- [x] **T2-13** `TeachingMaterialLibrary`：開課單位欄＋篩選；刪除 disable。
- [x] **T2-14** `types/materials.ts`／`authGuards.ts`。

---

## 三、教材套組編輯鎖定 ＋ 檔案檢視刪除（Phase 3）

- [x] **T3-1** `TeachingMaterialLibrary`：編輯中鎖定其他列編輯／刪除／新增；不鎖下載。
- [x] **T3-2** 檔案檢視刪除＋確認 Modal；受編輯鎖與 owner 判斷。

---

## 四、驗證（Phase 4）

| ID | 情境 | 預期 | 狀態 |
|----|------|------|------|
| **T4-1** | pytest（含 owner／dept_id／冪等／question_count） | 通過 | ✅ |
| **T4-2** | 上課 QR → 未登入 → login+returnTo → 自動報到 | 成功 | ☐ 待人工 |
| **T4-3** | 請假：上課未報到 → 考試前再產 QR → 補報到且不重複 | 成功 | ☐ 待人工 |
| **T4-4** | 已報到進考試中心 | 不再要求報到 | ☐ 待人工 |
| **T4-5** | 無考題計畫 QR 報到 | 不顯示「開始考試」／不導向 `/exam/run` | ☐ 待人工 |
| **T4-6** | 非 owner 刪除 | 前端 disable＋後端 403 | ☐ 待人工（單元測試已覆蓋） |
| **T4-7** | 超管刪除他人資料 | 允許 | ☐ 待人工（單元測試已覆蓋） |
| **T4-8** | 教材庫編輯鎖＋檔案刪除 | 符合 T3 | ☐ 待人工 |
| **T4-9** | `npm run lint`／`npm run build` | 通過 | ⚠️ 既有 `@shared-ui` 缺檔／無關 TS／eslint 錯誤；本波改動檔無新增 lint 問題 |

---

## 實作摘要

| 類別 | 內容 |
|------|------|
| 分支 | `cursor/checkin-owner-material-3b7d`（自 `feature/checkin-owner-scope-20260717`） |
| 本波補齊 | T1-2 TrainingPlanManager QR；T1-1b／T1-6 `question_count`／`has_exam`；TASKS 對齊實作計畫 |
| 遷移 | `add_owner_dept_fields.py`、`add_attendance_emp_plan_unique.py` |
| 測試 | `test_owner_scope`、`test_attendance_checkin_idempotent`、`test_attendance_question_count`、`test_exam_list_materials_nas` |
| 待人工 | T4-2～T4-8 |
