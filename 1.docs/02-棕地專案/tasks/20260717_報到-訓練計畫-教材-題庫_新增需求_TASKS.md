# 報到／訓練計畫／教材／題庫 新增需求 — 實作任務清單

- **需求來源／PLAN**：[../plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md](../plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md)
- **交付摘要**：[../交付實作文件/20260718_報到QR-開課單位Owner-教材庫UX.md](../交付實作文件/20260718_報到QR-開課單位Owner-教材庫UX.md)
- **狀態**：已完成程式實作（分支 `feature/checkin-owner-scope-20260717`，commit `407e527`～`f21478b`）；T4-2～T4-4／T4-6 待人工瀏覽器驗證後合併。

---

## 0. 已與使用者確認的範圍決策

- [x] 報到 QRcode：**訓練計畫管理操作欄**＋**報到總覽統計 Modal「顯示 QRcode」**；系統管理「QRcode 管理」維持純登入 QR。
- [x] 教材套組編輯鎖定**只做前端 UI 互斥**，不做後端跨使用者鎖。
- [x] Owner 例外角色為**超管／系統管理**（`is_management_role`）。
- [x] `dept_id` nullable、NULL＝不受 owner 限制。
- [x] **不新增 `checkin_type`**：同一組 QR＋單一 `attendance_records`。
- [x] Owner 套用：訓練計畫、QuestionBank、TeachingMaterialSet／File、**考卷工坊計畫考題**；**寫入**＝編輯＋刪除（計畫另含封存、產生報到 QR）。
- [x] 非 Owner **僅檢視／下載**；訓練計畫開啟為檢視模式；考卷工坊 `isWriteLocked`。
- [x] 報到未到原因／列印清冊**不**套用 Owner。
- [x] 檔案檢視補刪除；Owner＋超管可刪（軟刪）。
- [x] 當天請假／遲到：考試前再掃同一 QR 補報到。
- [x] `list_materials` NAS 不可達 → 200 空陣列 + `X-NAS-Unavailable`（不中斷題目管理）。

---

## 一、報到管理：上課前／考試時 一體化

### 後端
- [x] **T1-1** `POST /training/plans/{plan_id}/checkin-qrcode/generate` 重用（含 Owner 檢查）。
- [x] **T1-1b** `GET/POST .../attendance/status|checkin` 回傳 `plan_title`（員工無 `menu:plan` 也能顯示計畫名稱）。
- [x] **T1-1c** checkin **冪等**（已報到／IntegrityError 競態皆 200）；model `UNIQUE(emp_id, plan_id)`。
- [x] **T1-1d** migration `add_attendance_emp_plan_unique.py`（去重＋唯一索引）。

### 前端
- [x] **T1-2** `TrainingPlanManager.tsx`：操作欄「產生 QRcode」＋ Modal。
- [x] **T1-2b** `AttendanceOverviewPage.tsx`：統計 Modal「顯示／隱藏 QRcode」、更新鈕；QR 區放大、四狀態卡縮小（開 QR 時）；非 Owner disable 產生 QR。
- [x] **T1-3** `App.tsx`：`RedirectToLoginWithReturnTo`（未登入帶 `returnTo`）。
- [x] **T1-3b** `App.tsx`：`LoginReturnRedirect`（登入後 `setUser` 仍尊重 `returnTo`，修競態導回考試中心）。
- [x] **T1-4** `LoginPage.tsx`：登入成功讀取白名單 `returnTo`。
- [x] **T1-5** `CheckInPage.tsx`：自動報到；模組級 Promise 防 Strict Mode 雙寫；成功頁顯示 `plan_title`。

---

## 二、擁有權（開課單位＝Owner）

### 後端 — 資料庫與 Model
- [x] **T2-1** `migrations/add_owner_dept_fields.py`。
- [x] **T2-2** `models.py`：`QuestionBank`、`TeachingMaterialSet` 加 `dept_id`。

### 後端 — Owner 判斷邏輯
- [x] **T2-3** `access_scope.can_modify_owned_resource`（`can_delete_owned_resource` 別名）。

### 後端 — 套用點（寫入 API）
- [x] **T2-4** `training.py`：`PUT`、刪除、封存／取消封存、產生報到 QR。
- [x] **T2-5** `question_bank.py`：`PUT`、刪除／批次刪除、`POST /import`（匯入至計畫）。
- [x] **T2-6** `teaching_material_sets.py`：`PUT`、綁定計畫、上傳檔、刪除套組／檔。
- [x] **T2-7** `exam.py`：上傳／預覽匯入、題目 `PUT`／刪除、刪考卷 TXT（`_require_plan_exam_modify`）。

### 前端 — 檢視模式／disabled
- [x] **T2-9** `TrainingPlanManager`：`isViewOnly`、`fieldset disabled`、封存／刪除 disable。
- [x] **T2-10** `QuestionBankManager`：編輯／刪除 disable。
- [x] **T2-11** `TeachingMaterialLibrary`、`MaterialSetEditPanel`、`PlanMaterialsSection`。
- [x] **T2-12** `ExamStudio`：`isOwnerReadOnly`、`isWriteLocked`、琥珀色提示。
- [x] **T2-13** `authGuards.canModifyOwnedResource`；題庫／套組開課單位欄與搜尋。
- [x] **T2-14** `AttendanceOverviewPage`：非 Owner 不可產生 QR。

---

## 三、教材套組編輯鎖定 ＋ 檔案檢視刪除

- [x] **T3-1** `TeachingMaterialLibrary`：`rowActionsLocked`。
- [x] **T3-2** 檔案檢視刪除＋確認 Modal（軟刪說明）。

---

## 四、考卷工坊 NAS 列表

- [x] **T4-N1** `exam.list_materials`：NAS 不可達 → 200 `[]` + `X-NAS-Unavailable: 1`。
- [x] **T4-N2** `main.py` CORS `expose_headers`。
- [x] **T4-N3** `ExamStudio` 琥珀色 `materialsNasWarning`。
- [x] **T4-N4** `tests/test_exam_list_materials_nas.py`。

---

## 五、驗證

- [x] **T4-1** pytest：`test_owner_scope.py`（14 項）、`test_attendance_checkin_idempotent.py`、`test_exam_list_materials_nas.py`。
- [ ] **T4-2** 手動報到：未登入掃 QR → returnTo → 自動報到 → 成功頁有計畫名稱 → 統計一筆。
- [ ] **T4-3** 手動 Owner：非開課單位無法編輯／刪除／封存（計畫／題庫／教材／**考卷工坊考題**）；超管可。
- [ ] **T4-4** 手動教材庫：編輯鎖、檔案刪除。
- [x] **T4-5** `npm run lint`／`npm run build`（既有無關 lint 除外）。
- [ ] **T4-6** 手動：統計 Modal QR 版面（左大右小四卡）。

---

## 實作摘要

| 類別 | 內容 |
|------|------|
| 分支 | `feature/checkin-owner-scope-20260717` |
| 關鍵 commit | `407e527`（報到 QR／Owner 初版）→ `f21478b`（寫入權擴充＋考卷工坊 Owner＋NAS list） |
| 關鍵修補 | `LoginReturnRedirect`；attendance UNIQUE＋冪等；`plan_title`；Modal QR 版面；`can_modify_owned_resource`；ExamStudio 檢視模式 |
| 遷移 | `add_owner_dept_fields.py`、`add_attendance_emp_plan_unique.py` |
| 文件 | PLAN、本 TASKS、交付實作文件、`專案使用說明`、`MIGRATION_GUIDE`、`生產部署指南`、`棕地功能總覽` |
| 待人工 | T4-2／T4-3／T4-4／T4-6 |
