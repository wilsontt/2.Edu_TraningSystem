# 報到／訓練計畫／教材／題庫 新增需求 — 實作任務清單

- **需求來源**：`1.docs/02-棕地專案/plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md`
- **對應完整計畫**：本任務清單為該計畫的可執行拆解，設計理由與現況分析詳見計畫全文（如需要可另外整理成 `1.docs` 內的技術設計文件）。
- **狀態**：草稿，待對齊；尚未開始實作。

---

## 0. 已與使用者確認的範圍決策

- [ ] （僅供對齊確認，非開發任務）報到 QRcode 產生功能**只放在「訓練計畫管理」操作欄**，重用既有孤兒 API；系統管理「QRcode 管理」維持純登入 QR、不修改。
- [ ] （僅供對齊確認）教材套組編輯鎖定**只做前端 UI 互斥**，不做後端跨使用者鎖。
- [ ] （僅供對齊確認）Owner 限制的例外角色為**超管／系統管理角色**（`is_management_role`）。
- [ ] （僅供對齊確認）新增的 `dept_id` 欄位一律 nullable、預設 NULL，NULL＝不受 owner 限制；上線前既有測試資料會清空，不需人工回填流程。
- [ ] （僅供對齊確認）**不新增 `checkin_type` 欄位**：「上課前／考試時」報到以「同一組 QR、可重複開啟」＋「單一報到記錄」實現，不拆表、不加型態欄位（若日後報到總覽要顯示報到管道，屬於獨立需求）。
- [ ] （僅供對齊確認）Owner 限制只套用在「訓練計畫」「歷史題庫 QuestionBank」「教材套組 TeachingMaterialSet／File」，不擴及考卷工坊內尚未進題庫的 `Question`。

---

## 一、報到管理：上課前／考試時 一體化

### 後端
- [ ] **T1-1** 確認 `POST /training/plans/{plan_id}/checkin-qrcode/generate`（`backend/app/routers/training.py` ~L951-1017）維持現狀可直接重用，不需改動邏輯，僅需前端接上。

### 前端
- [ ] **T1-2** `frontend/src/components/admin/TrainingPlanManager.tsx`：清單操作欄新增「產生 QRcode」按鈕，呼叫 `checkin-qrcode/generate`，Modal 顯示 QR 圖片＋可複製連結。可參考 commit `bbb0a02` 移除前的實作（`checkinQRCode`/`generatingQRCode`/`copiedCheckinUrl` state 與 JSX），搬到操作欄。
- [ ] **T1-3** `frontend/src/App.tsx`：未登入導向 `/login` 時（~L342-344）帶上 `returnTo`（僅允許 `/checkin` 開頭的相對路徑，避免 open redirect），需引入 `useLocation()`。
- [ ] **T1-4** `frontend/src/components/LoginPage.tsx`（~L86-87）：登入成功後改讀取白名單過的 `returnTo` 並導頁，否則維持導向 `/`。
- [ ] **T1-5** `frontend/src/components/exam/CheckInPage.tsx`：報到狀態查詢後，若未報到則**自動**呼叫一次 `handleCheckIn`（不需使用者手動按鈕），失敗時顯示錯誤並保留手動重試按鈕。

---

## 二、擁有權（開課單位＝Owner）

### 後端 — 資料庫與 Model
- [ ] **T2-1** 新增 migration `backend/migrations/add_owner_dept_fields.py`（比照 `backend/migrations/add_ad_auth_user_fields.py` 的 idempotent 寫法）：
  - `ALTER TABLE question_bank ADD COLUMN dept_id INTEGER REFERENCES departments(id);`
  - `ALTER TABLE teaching_material_sets ADD COLUMN dept_id INTEGER REFERENCES departments(id);`
- [ ] **T2-2** `backend/app/models.py`：`QuestionBank`、`TeachingMaterialSet` 加上 `dept_id` 欄位與 `department` relationship。

### 後端 — Owner 判斷邏輯
- [ ] **T2-3** `backend/app/access_scope.py`：新增 `can_delete_owned_resource(current_user, resource_dept_id)`，NULL 放行、`is_management_role()` 放行、否則比對 `current_user.dept_id`。

### 後端 — 套用點
- [ ] **T2-4** `backend/app/routers/training.py` `delete_training_plan()`（~L327）：既有業務規則檢查後加 owner 檢查。
- [ ] **T2-5** `backend/app/routers/exam.py`：`POST /admin/exams/upload`（~L192-226）與 `POST /admin/exams/import-from-preview`（~L302-322）寫入 `QuestionBank` 時帶入 `dept_id=plan.dept_id`。
- [ ] **T2-6** `backend/app/routers/question_bank.py`：`GET /admin/question-bank/`（L17）加 `dept_id` 篩選參數；`DELETE /admin/question-bank/{id}`（L121）、`DELETE .../bulk-delete`（L90）加 owner 檢查。
- [ ] **T2-7** `backend/app/routers/teaching_material_sets.py`：
  - `create_set`（L96）新增必填 `dept_id` 參數
  - `update_set`（L294）允許改 `dept_id`（受 owner 檢查）
  - `list_sets`（L231）、`list_files`（L506）加 `dept_id` 篩選參數
  - `delete_set`（L344）、`remove_set_file`（L484）加 owner 檢查（檔案比對其所屬 set 的 `dept_id`）
- [ ] **T2-8** `backend/app/schemas.py`：`QuestionBank`／`TeachingMaterialSetCreate`／`Update`／`TeachingMaterialSetListItemOut`／`TeachingMaterialFileListItemOut` 加 `dept_id`（＋ join 出的 `dept_name`）。

### 前端
- [ ] **T2-9** `frontend/src/components/admin/QuestionBankManager.tsx`：搜尋列加「開課單位」下拉、表格加欄位、刪除按鈕依 owner 判斷 disable。
- [ ] **T2-10** `frontend/src/components/teaching/MaterialSetUploadPanel.tsx`：獨立新增時加必填「開課單位」下拉；由 `PlanMaterialsSection.tsx`（`lockedPlanId`）呼叫時自動帶入該計畫 `dept_id` 並鎖定唯讀。
- [ ] **T2-11** `frontend/src/components/teaching/MaterialSetEditPanel.tsx`：加開課單位欄位（僅 owner/超管可編輯）。
- [ ] **T2-12** `frontend/src/App.tsx` → `ExamStudio.tsx` → `TeachingMaterialLibrary.tsx`：新增 `user` prop 傳遞鏈，供前端 owner 判斷使用。
- [ ] **T2-13** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：`setColumns`／`fileColumns` 加「開課單位」欄＋搜尋篩選；刪除按鈕依 owner 判斷 disable。
- [ ] **T2-14** `frontend/src/types/materials.ts`：同步加 `dept_id`/`dept_name` 型別欄位。

> 三、歷史題庫／教材庫清單欄位與搜尋，已包含在 T2-6、T2-7、T2-9、T2-13 內，不重複列出。

---

## 三、教材套組編輯鎖定 ＋ 檔案檢視刪除

- [ ] **T3-1** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：擴充編輯互斥邏輯 — `editingSetId != null` 或 `uploadOpen` 時，所有列的編輯／刪除按鈕與「新增教材套組」按鈕皆 disabled；下載/預覽不鎖定；取消編輯後恢復。
- [ ] **T3-2** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：`fileColumns`（~L320-384）新增「刪除」按鈕，呼叫既有 `removeSetFile(setId, fileId)` → `DELETE /admin/teaching-materials/sets/{set_id}/files/{file_id}`（`remove_set_file`，已於 T2-7 加上 owner 檢查）。確認彈窗可仿 `MaterialSetEditPanel.tsx`（L358-389）既有的 `pendingRemove` 樣式。刪除按鈕同時受 T3-1 編輯鎖與 owner 判斷影響。

---

## 四、驗證

- [ ] **T4-1** `backend/.venv/bin/python3 -m pytest tests/ -q` 全數通過；補充 owner 檢查與 `dept_id` 篩選測試（可仿 `tests/test_teaching_material_sets_api.py`、`tests/test_teaching_material_set_models.py`）。
- [ ] **T4-2** 手動跑通報到流程：訓練計畫管理按「產生 QRcode」→ 無痕視窗掃碼/開連結（模擬未登入）→ 導向登入頁並帶 `returnTo` → 登入後自動完成報到 → 再開同連結顯示已報到 → 進入該計畫開始考試不再要求報到。
- [ ] **T4-3** 手動驗證 Owner 限制：非開課單位帳號刪除他人資料應被拒絕（前端隱藏＋後端 403）；超管角色可正常刪除。
- [ ] **T4-4** 手動驗證教材庫：編輯中其他列鎖定／取消後解除；檔案檢視頁籤刪除單一檔案正常。
- [ ] **T4-5** `npm run lint`、`npm run build`（`frontend/`）通過。
