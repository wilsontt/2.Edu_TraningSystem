# 報到／訓練計畫／教材／題庫 新增需求 — 實作任務清單

- **需求來源**：`1.docs/02-棕地專案/plans/20260717_報到-訓練計畫-教材-題庫_新增需求_PLAN.md`
- **對應完整計畫**：本任務清單為該計畫的可執行拆解，設計理由與現況分析詳見計畫全文（如需要可另外整理成 `1.docs` 內的技術設計文件）。
- **狀態**：已完成實作（分支 `feature/checkin-owner-scope-20260717`），待人工瀏覽器驗證後合併。

---

## 0. 已與使用者確認的範圍決策

- [x] （僅供對齊確認，非開發任務）報到 QRcode 產生功能**只放在「訓練計畫管理」操作欄**，重用既有孤兒 API；系統管理「QRcode 管理」維持純登入 QR、不修改。
- [x] （僅供對齊確認）教材套組編輯鎖定**只做前端 UI 互斥**，不做後端跨使用者鎖。
- [x] （僅供對齊確認）Owner 限制的例外角色為**超管／系統管理角色**（`is_management_role`）。
- [x] （僅供對齊確認）新增的 `dept_id` 欄位一律 nullable、預設 NULL，NULL＝不受 owner 限制；上線前既有測試資料會清空，不需人工回填流程。
- [x] （僅供對齊確認）**不新增 `checkin_type` 欄位**：「上課前／考試時」報到以「同一組 QR、可重複開啟」＋「單一報到記錄」實現，不拆表、不加型態欄位。
- [x] （僅供對齊確認）Owner 限制只套用在「訓練計畫」「歷史題庫 QuestionBank」「教材套組 TeachingMaterialSet／File」，不擴及考卷工坊內尚未進題庫的 `Question`。

---

## 一、報到管理：上課前／考試時 一體化

### 後端
- [x] **T1-1** 確認 `POST /training/plans/{plan_id}/checkin-qrcode/generate`（`backend/app/routers/training.py` ~L973-1017）維持現狀可直接重用，不需改動邏輯，僅前端接上。

### 前端
- [x] **T1-2** `frontend/src/components/admin/TrainingPlanManager.tsx`：清單操作欄新增「產生 QRcode」按鈕（`QrCode` icon），呼叫 `checkin-qrcode/generate`，獨立 Modal 顯示 QR 圖片＋可複製連結（`qrCodeTarget`/`checkinQRCode`/`generatingQRCode`/`copiedCheckinUrl` state）。
- [x] **T1-3** `frontend/src/App.tsx`：新增 `RedirectToLoginWithReturnTo` 元件，未登入導向 `/login` 時帶 `returnTo`（僅允許 `/checkin` 開頭路徑，避免 open redirect）。
- [x] **T1-4** `frontend/src/components/LoginPage.tsx`：`handleLoginSuccess` 讀取白名單過的 `returnTo` 並導頁，否則維持導向 `/`。
- [x] **T1-5** `frontend/src/components/exam/CheckInPage.tsx`：報到狀態查詢後，若未報到則自動呼叫 `handleCheckIn`（`useCallback` 化），失敗時顯示錯誤並保留手動重試按鈕。

---

## 二、擁有權（開課單位＝Owner）

### 後端 — 資料庫與 Model
- [x] **T2-1** 新增 migration `backend/migrations/add_owner_dept_fields.py`（idempotent，已於本機資料庫執行並驗證可重複執行）。
- [x] **T2-2** `backend/app/models.py`：`QuestionBank`、`TeachingMaterialSet` 加上 `dept_id` 欄位與 `department` relationship（`QuestionBank` 另加 `dept_name` property）。

### 後端 — Owner 判斷邏輯
- [x] **T2-3** `backend/app/access_scope.py`：新增 `can_delete_owned_resource(current_user, resource_dept_id)`，NULL 放行、`is_management_role()` 放行、否則比對 `current_user.dept_id`。

### 後端 — 套用點
- [x] **T2-4** `backend/app/routers/training.py` `delete_training_plan()`：既有業務規則檢查後加 owner 檢查（403）。
- [x] **T2-5** `backend/app/routers/exam.py`：`POST /admin/exams/upload` 與 `POST /admin/exams/import-from-preview` 寫入 `QuestionBank` 時帶入 `dept_id=plan.dept_id`。
- [x] **T2-6** `backend/app/routers/question_bank.py`：`GET /admin/question-bank/` 加 `dept_id` 篩選參數；`DELETE .../{id}`、`DELETE .../bulk-delete`（回傳 `denied_ids`）加 owner 檢查。
- [x] **T2-7** `backend/app/routers/teaching_material_sets.py`：
  - `create_set` 新增必填 `dept_id` 參數（驗證部門存在）
  - `update_set` 允許改 `dept_id`（受 owner 檢查，非 owner 嘗試變更回 403）
  - `list_sets`、`list_files` 加 `dept_id` 篩選參數
  - `delete_set`、`remove_set_file` 加 owner 檢查（檔案比對其所屬 set 的 `dept_id`）
- [x] **T2-8** `backend/app/schemas.py`：`QuestionBank`／`TeachingMaterialSetOut`／`TeachingMaterialSetUpdate`／`TeachingMaterialFileListItemOut` 加 `dept_id`（＋ `dept_name`）。

### 前端
- [x] **T2-9** `frontend/src/components/admin/QuestionBankManager.tsx`：搜尋列加「開課單位」下拉、表格加欄位、刪除按鈕（單筆＋批次全選）依 owner 判斷 disable。
- [x] **T2-10** `frontend/src/components/teaching/MaterialSetUploadPanel.tsx`：獨立新增時加必填「開課單位」下拉；由 `PlanMaterialsSection.tsx`（`lockedDeptId`）呼叫時自動帶入該計畫 `dept_id` 並鎖定唯讀。
- [x] **T2-11** `frontend/src/components/teaching/MaterialSetEditPanel.tsx`：加開課單位欄位（僅 owner/超管可編輯，非 owner 時 disabled 且不送出 `dept_id` 避免誤觸後端 403）。
- [x] **T2-12** `frontend/src/App.tsx` → `TrainingPlanManager.tsx`／`ExamStudio.tsx` → `TeachingMaterialLibrary.tsx`／`QuestionBankManager.tsx`：新增 `user` prop 傳遞鏈，供前端 owner 判斷使用（`utils/authGuards.ts` 新增 `canDeleteOwnedResource()`；`/auth/me` 與 `User` 型別新增 `dept_id`）。
- [x] **T2-13** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：`setColumns`／`fileColumns` 加「開課單位」欄＋搜尋篩選；刪除按鈕依 owner 判斷 disable。
- [x] **T2-14** `frontend/src/types/materials.ts`：同步加 `dept_id`/`dept_name` 型別欄位，新增 `DepartmentOption` 型別。

> 三、歷史題庫／教材庫清單欄位與搜尋，已包含在 T2-6、T2-7、T2-9、T2-13 內，不重複列出。

---

## 三、教材套組編輯鎖定 ＋ 檔案檢視刪除

- [x] **T3-1** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：新增 `rowActionsLocked = editingSetId != null || uploadOpen`；套組列與檔案列的編輯／刪除按鈕皆依此 disabled；下載/預覽不鎖定；取消編輯或關閉新增後自動恢復。
- [x] **T3-2** `frontend/src/components/teaching/TeachingMaterialLibrary.tsx`：`fileColumns` 新增「刪除」按鈕，呼叫既有 `removeSetFile(setId, fileId)`；確認彈窗仿 `MaterialSetEditPanel.tsx` 既有的 `pendingRemove` 樣式（新增 `pendingRemoveFile` state）。刪除按鈕同時受 T3-1 編輯鎖與 owner 判斷影響。

---

## 四、驗證

- [x] **T4-1** `backend/.venv/bin/python3 -m pytest tests/ -q`：92 passed（含既有 84 + 新增 `tests/test_owner_scope.py` 8 項：`can_delete_owned_resource()` 純函式測試、`dept_id` 篩選測試、owner 拒絕刪除測試）；另因 `create_set` 新增必填 `dept_id`，同步修正 `tests/test_teaching_material_sets_*.py`／`test_teaching_materials_router_wave1_removed.py` 既有測試的 form data。`test_error_detail.py`／`test_exam_upload.py`／`test_new_apis.py` 為既有需真實伺服器之手動煙測腳本，非本次變更所致，維持原狀。
- [ ] **T4-2** 手動跑通報到流程（**待人工瀏覽器驗證**；後端 API 層已以臨時腳本驗證 QRcode 產生→報到狀態查詢→報到→再查詢 全部正確，已移除該臨時腳本）：訓練計畫管理按「產生 QRcode」→ 無痕視窗掃碼/開連結（模擬未登入）→ 導向登入頁並帶 `returnTo` → 登入後自動完成報到 → 再開同連結顯示已報到 → 進入該計畫開始考試不再要求報到。
- [ ] **T4-3** 手動驗證 Owner 限制（**待人工瀏覽器驗證**；後端 403 邏輯已由 `tests/test_owner_scope.py` 涵蓋）：非開課單位帳號刪除他人資料應被拒絕（前端隱藏＋後端 403）；超管角色可正常刪除。
- [ ] **T4-4** 手動驗證教材庫（**待人工瀏覽器驗證**）：編輯中其他列鎖定／取消後解除；檔案檢視頁籤刪除單一檔案正常。
- [x] **T4-5** `npm run lint`、`npm run build`（`frontend/`）通過；僅剩 5 項與本次變更無關之既有 lint 錯誤（`PlanBindingChecklist.tsx` 3 項、`PlanMaterialsSection.tsx` 2 項，皆為既有 `react-hooks/set-state-in-effect` 規則違規，非本次新增）。

---

## 實作摘要

- **分支**：`feature/checkin-owner-scope-20260717`（自 `main` 切出）。
- **後端變更檔**：`access_scope.py`、`models.py`、`schemas.py`、`routers/auth.py`（`/auth/me` 加 `dept_id`）、`routers/training.py`、`routers/exam.py`、`routers/question_bank.py`、`routers/teaching_material_sets.py`；新增 `migrations/add_owner_dept_fields.py`。
- **前端變更檔**：`App.tsx`、`LoginPage.tsx`、`components/admin/TrainingPlanManager.tsx`、`components/admin/ExamStudio.tsx`、`components/admin/QuestionBankManager.tsx`、`components/exam/CheckInPage.tsx`、`components/teaching/{PlanMaterialsSection,MaterialSetUploadPanel,MaterialSetEditPanel,TeachingMaterialLibrary}.tsx`、`api/teachingMaterials.ts`、`types.ts`、`types/materials.ts`、`utils/authGuards.ts`。
- **測試變更**：新增 `tests/test_owner_scope.py`；修正 5 個既有教材套組測試檔以配合 `dept_id` 必填。
- **文件同步**：`1.docs/00-專案總覽/專案使用說明.md`（§3.2 產生 QRcode／Owner 說明、§3.4 題庫開課單位、§3.6 教材庫 Owner／鎖定、§4.2 報到雙時機 SOP）、`1.docs/00-專案總覽/資料庫遷移/MIGRATION_GUIDE.md`（新增「開課單位擁有權（2026-07-17）」小節）。
- **尚未完成**：T4-2／T4-3／T4-4 之瀏覽器手動點擊驗證（需啟動前後端伺服器並以實際帳號操作，非本次自動化工具可涵蓋，請於合併前由人工執行）。
