# 合併報到批次與報到歷程 — 實作任務清單

- **PLAN**：[../plans/20260720_合併報到批次與報到歷程_PLAN.md](../plans/20260720_合併報到批次與報到歷程_PLAN.md)
- **狀態**：✅ 程式已實作（分支 `cursor/batch-checkin-history-3b7d`）；人工驗收待勾

---

## 0. 已定案決策（勿改）

- [x] 勾選：≥2 場、進行中未封存；不限同一開始日；候選＝場次日區間 OR 關鍵字；已選購物車換條件不清空
- [x] 批次 `training_date`＝場次／報到日（可與計畫開始日不同）
- [x] 部分成功（後端）；UI 可簡化
- [x] 批次關閉 → QR 失效；可重開 + audit
- [x] `checkin_time` 不覆蓋；events append-only
- [x] 報到總覽 QR 不綁 Owner
- [x] 重考免報到

---

## Phase F1 — 資料庫與 Model

- [x] **T1-1** migration：`add_attendance_checkin_batch_and_events.py`
- [x] **T1-2** `models.py`：Batch／BatchPlan／Event
- [x] **T1-3** `schemas.py` 請求／回應結構

---

## Phase F2 — 後端 API

- [x] **T2-1** `POST /training/attendance/batches`
- [x] **T2-2** `GET .../batches/{id}`、`GET .../batches/{id}/stats`
- [x] **T2-3** `PATCH .../batches/{id}/status`（closed / reopened + audit log）
- [x] **T2-4** `POST /exam/attendance/batches/{batch_id}/checkin`
- [x] **T2-5** `GET /training/plans/{plan_id}/attendance/events`
- [x] **T2-6** 單計畫 checkin 雙寫 events
- [x] **T2-7** `checkin-qrcode/generate`：overview 不檢 Owner

---

## Phase F3 — 前端

- [x] **T3-1** 報到總覽：合併報到按鈕、勾選 Modal、大 QR Modal
- [x] **T3-2** 關閉／重開、stats polling
- [x] **T3-3** 移除單計畫 QR Owner disable
- [x] **T3-4** `CheckInPage`：`batch_id`
- [x] **T3-5** `returnTo` 已支援 `/checkin?batch_id=`
- [x] **T3-6** 報到統計 Modal：歷程折疊

---

## Phase F4 — 測試與文件

- [x] **T4-1** pytest：`test_attendance_batch_checkin.py` 等
- [ ] **T4-2** lint／build（既有 `@shared-ui` 環境限制）
- [x] **T4-3** MIGRATION_GUIDE、專案使用說明、本 TASKS／PLAN 狀態
- [ ] **T4-4** 人工驗收 PLAN §7

---

## Phase F2（選做，不阻塞 MVP）

- [ ] **T5-1** 批次歷史管理獨立頁
- [ ] **T5-2** 合併 QR 列印／海報
