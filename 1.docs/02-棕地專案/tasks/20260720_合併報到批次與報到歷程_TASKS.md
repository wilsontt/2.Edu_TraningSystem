# 合併報到批次與報到歷程 — 實作任務清單

- **PLAN**：[../plans/20260720_合併報到批次與報到歷程_PLAN.md](../plans/20260720_合併報到批次與報到歷程_PLAN.md)
- **狀態**：📋 待實作

---

## 0. 已定案決策（勿改）

- [x] 勾選：≥2 場、active、同一 `training_date`
- [x] 部分成功（後端）；UI 可簡化
- [x] 批次關閉 → QR 失效；可重開 + audit
- [x] `checkin_time` 不覆蓋；events append-only
- [x] 報到總覽 QR 不綁 Owner
- [x] 重考免報到

---

## Phase F1 — 資料庫與 Model

- [ ] **T1-1** migration：`attendance_checkin_batches`、`attendance_checkin_batch_plans`、`attendance_checkin_events`
- [ ] **T1-2** `models.py` SQLAlchemy 模型 + relationships
- [ ] **T1-3** `schemas.py` 請求／回應結構

---

## Phase F2 — 後端 API

- [ ] **T2-1** `POST /training/attendance/batches`（驗證 + QR 產生）
- [ ] **T2-2** `GET .../batches/{id}`、`GET .../batches/{id}/stats`
- [ ] **T2-3** `PATCH .../batches/{id}/status`（closed / reopened + audit_log）
- [ ] **T2-4** `POST /exam/attendance/batches/{batch_id}/checkin`（部分成功）
- [ ] **T2-5** `GET /training/plans/{plan_id}/attendance/events`
- [ ] **T2-6** 單計畫 `checkin` 改為雙寫 events（success / already_checked）
- [ ] **T2-7** 修正 `checkin-qrcode/generate` 權限與 Owner（報到總覽不檢 Owner）

---

## Phase F3 — 前端

- [ ] **T3-1** `AttendanceOverviewPage`：合併報到按鈕、勾選 Modal、大 QR Modal
- [ ] **T3-2** 大 Modal：關閉／重開、右側 stats polling、QR 關閉後灰顯
- [ ] **T3-3** 移除報到總覽單計畫 QR 之 Owner disable
- [ ] **T3-4** `CheckInPage`：`batch_id`、成功／失敗文案
- [ ] **T3-5** `LoginPage` / `App.tsx`：`returnTo` 支援 `batch_id`
- [ ] **T3-6** 報到統計 Modal：歷程折疊區（events + 未到原因）

---

## Phase F4 — 測試與文件

- [ ] **T4-1** pytest：批次建立、checkin、closed、reopen、events、Owner 修正
- [ ] **T4-2** `npm run lint` / `npm run build`
- [ ] **T4-3** 同步 MIGRATION_GUIDE、專案使用說明、棕地總覽、交付文件
- [ ] **T4-4** 人工驗收 PLAN §7 T1～T9

---

## Phase F2（選做，不阻塞 MVP）

- [ ] **T5-1** 批次歷史管理獨立頁
- [ ] **T5-2** 合併 QR 列印／海報
