# 合併報到未到原因、歷程時間軸與批次紀錄 — 實作計劃 (PLAN)

**文件類型**：棕地實作計劃  
**建立日期**：2026-07-22  
**狀態**：⏳ 待審核（請確認後再實作）  
**前置 PLAN**：[20260720_合併報到批次與報到歷程_PLAN.md](20260720_合併報到批次與報到歷程_PLAN.md)（合併 QR／events／關閉重開；已合併至 `main`）  
**任務清單**：待 PLAN 核准後建立 `../tasks/20260722_合併報到未到原因與歷程時間軸_TASKS.md`  
**本機部署**：驗收在**使用者本機**（非 Cloud Agent workspace）；須 `git pull` + migration + 前後端重建／重啟

> **與 20260720 之關係**：20260720 已交付「合併 QR + checkin events + 標籤必填 + 歷程中文」。本 PLAN 補齊課後作業缺口：**未到原因與合併批次語意對齊**、**歷程真正時間軸**、**關掉 Modal 後仍可找回批次**。

---

## 1. 目的

1. **課後未到原因不必靠記憶**：課程結束後填未到原因時，承辦人從「合併報到紀錄」進入；系統依 `batch → plans` 自動知道要套用到哪幾場，**不要求記住計畫名稱**。
2. **合併 N 場、未到原因填一次**：同一人因故未到，在批次入口填一次原因，自動套用到該 batch 內所有「應到且未到」的計畫；單一計畫可覆寫例外。
3. **報到歷程＝時間軸**：主列表「報到時間」顯示**最後一次狀態時間**；展開歷程依**新→舊（3→2→1）**顯示；含公假／病假變更與最終準時報到。
4. **資料可稽核**：未到原因變更改為 append-only 寫入歷程事件，不再只覆蓋現況一筆而失去歷史。

---

## 2. 範圍

### 2.1 涵蓋（In Scope）

| Phase | 摘要 |
|-------|------|
| **G1 合併報到紀錄** | 報到總覽新增「合併報到紀錄」列表／區塊；可依場次日、標籤搜尋；可重新開啟已關閉批次詳情（填未到原因、查看統計、重開 QR） |
| **G2 批次層未到原因** | `batch` 層寫入／批次套用 API；一次填寫 → 寫入 batch 內各未到 plan 的 reason（或 batch-level 表 + plan override） |
| **G3 歷程時間軸** | events 排序改降冪；主列顯示 `latest_event_time`；未到原因變更寫入 events；前端中文時間軸 |
| **G4 單一計畫銜接** | 計畫「報到統計」Modal 顯示「曾參與合併報到：{標籤}」並可跳轉批次入口；plan 可個別覆寫原因 |
| **G5 本機部署文件** | 交付文件＋部署指南補：pull、migration、本機 Vite／Docker 重啟驗收清單 |

### 2.2 不涵蓋（Out of Scope）

| 項目 | 說明 |
|------|------|
| 自動 TTL 關批次 | 仍手動關閉／重開 |
| 刪除／改寫既有 events | events 仍 append-only、不可硬刪 |
| 跨日自動合併未到原因 | 僅針對**同一 batch** 綁定的計畫 |
| 已報到人員再填未到原因 | 僅「應到且未到」可填；已報到維持現況 |
| 改動考試中心開考鐵律 | 不影響「須先報到才能開考」 |

### 2.3 審核決策（待確認 — 請勾選／修改）

| # | 提案 | 預設建議 | 請確認 |
|---|------|----------|--------|
| Q1 | 課後填未到原因的**主入口** | **合併報到紀錄**（非單一計畫 Modal） | ☐ OK／☐ 改：___ |
| Q2 | 「填一次」寫入策略 | **方案 B（推薦）**：新增 `attendance_batch_absence_reasons`；套用時同步寫入各 plan 的 `attendance_absence_reasons`（現況表仍可被 stats 讀取）；並 append event | ☐ OK／☐ 改採方案 A（只自動寫多筆 plan、不加 batch 表） |
| Q3 | 單一計畫原因與批次原因衝突 | **plan override 優先**；UI 標示「個別覆寫」 | ☐ OK／☐ 一律強制與 batch 相同 |
| Q4 | 關閉 QR／關閉 Modal 後 | 紀錄列表仍可開詳情填未到原因；**不必重開 QR** 也能填原因 | ☐ OK／☐ 必須先重開批次才能填 |
| Q5 | 主列表「報到時間」語意 | 顯示**最後一次歷程事件時間**（含原因變更或報到成功）；首次成功報到時間仍存在 `attendance_records.checkin_time`（稽核用，可於歷程內另標「首次報到」） | ☐ OK／☐ 主列仍只顯示首次 `checkin_time` |
| Q6 | 歷程排序 | **新→舊**（`event_time DESC, id DESC`） | ☐ OK |
| Q7 | 歷程是否含未到原因 | **要**；填／改／取消請假皆寫 event | ☐ OK |
| Q8 | 批次套用範圍 | 僅 `batch.plans` 內、該員為應到對象、且**尚未報到**的 plan | ☐ OK |
| Q9 | 標籤用途 | 繼續必填；作為紀錄列表主識別（找回批次的錨點） | ☐ OK |

---

## 3. 權責

| 角色 | 責任 |
|------|------|
| 產品／審核 | 確認 Q1～Q9；確認課後 SOP（誰、何時、從哪個入口填未到原因） |
| 開發 | PLAN 核准後依 TASKS 實作 migration、API、前端、pytest；同步文件 |
| 使用者／維運 | **本機** pull 指定 branch → migration → 重啟前後端 → 依驗收案例勾選 |

---

## 4. 名詞解釋

| 名詞 | 定義 |
|------|------|
| **合併報到批次 (batch)** | 一次 QR／一個標籤對應多個 `TrainingPlan`；關閉後 QR 不可報到，但紀錄仍可查 |
| **合併報到紀錄** | 報到總覽上可搜尋、可重開的 batch 列表（解決「Modal 關了就找不到」） |
| **批次層未到原因** | 以 `batch_id + emp_id` 表達「這次合併場次此人未到的原因」 |
| **計畫層未到原因** | 既有 `attendance_absence_reasons`（`plan_id + emp_id`）；可作現況與 override |
| **歷程事件 (event)** | append-only；含報到成功／已報到過／未到原因變更等 |
| **時間軸** | 依 `event_time` 新→舊列出該員在該 plan（或 batch 視角）之狀態變更 |

---

## 5. 作業內容

### 5.1 使用者流程（課後填未到原因）

```text
報到總覽
  →「合併報到紀錄」
  → 依場次日／標籤找到「0721 上午場」
  → 開啟批次詳情（顯示綁定的 A、B 計畫名稱）
  → 未到人員清單 → 選原因（病假／公假…）填一次
  → 系統套用到 A、B 中應到且未到者
  → 各計畫報到統計與歷程皆可見
```

**失敗情境（本 PLAN 要消除）**：關掉合併 Modal → 忘記是哪兩場 → 在單一計畫亂填或漏填。

### 5.2 資料設計（提案）

#### G2 方案 B（推薦）

| 表／欄位 | 用途 |
|----------|------|
| **新表** `attendance_batch_absence_reasons` | `batch_id`, `emp_id`, `reason_code`, `reason_text`, `recorded_by`, `recorded_at`（現況／預設） |
| **既有** `attendance_absence_reasons` | 套用時同步 upsert；plan 個別修改＝override |
| **擴充** `attendance_checkin_events` | 新增可選 `reason_code` / `reason_text` / `operator_emp_id`；`event_type` 含 `absence_reason_updated` / `absence_reason_cleared` |

#### 寫入規則（套用）

對 `batch` 內每個 `plan_id`：

1. 員工為該 plan 應到對象  
2. 無 `attendance_records`（未報到）  
3. → upsert plan-level reason  
4. → append event（同一操作時間可相同；每個 plan 各一筆，或一筆 batch 事件＋plan 展開，**實作時採「每 plan 一筆 event」以利單一計畫歷程一致**）

### 5.3 API（草案）

| Method | Path | 說明 |
|--------|------|------|
| GET | `/training/attendance/batches` | 列表：`training_date`、`keyword`（標籤）、`status`；回傳 plans 摘要、未到人數 |
| GET | `/training/attendance/batches/{id}` | 既有；詳情含 plans |
| GET | `/training/plans/{plan_id}/attendance/batches` | 該計畫曾參與的 batch（給單一 Modal 提示） |
| PUT | `/training/attendance/batches/{id}/absence-reason` | 單人批次套用 |
| PUT | `/training/attendance/batches/{id}/absence-reason/bulk` | 多人批次套用 |
| GET | `/training/plans/{plan_id}/attendance/events` | **改降冪**；回傳中文友善欄位／`batch_label`／`reason_*` |
| GET | `/training/plans/{plan_id}/attendance/stats` | `checked_in_users`／未到列補 `latest_event_time`（及可選 label） |

權限：沿用 `menu:attendance-overview`／`menu:plan`；未到原因編輯規則沿用現行 `_can_edit_absence_reason`。

### 5.4 前端（草案）

| 畫面 | 變更 |
|------|------|
| 報到總覽 | 新增「合併報到紀錄」區塊（日期＋標籤篩選、狀態、計畫名稱、開啟詳情） |
| 批次詳情 Modal | 除 QR／統計外，增加「未到人員／填寫原因」；已關閉仍可填原因（Q4） |
| 單計畫報到統計 | 頂部提示所屬 batch；歷程新→舊；主列最後事件時間 |
| 歷程文案 | 例：`準時報到`、`病假`、`公假`；合併來源可作次要括號 |

### 5.5 驗收案例（本機）

| # | 步驟 | 預期 |
|---|------|------|
| V1 | 建立 batch「0721 上午場」含 A+B → 關閉 Modal／關閉 QR | 合併報到紀錄仍可依標籤／日期找到 |
| V2 | 從紀錄開啟 → 對未到員工填一次「病假」 | A、B 該員皆顯示病假；不必填兩次 |
| V3 | 於 A 計畫個別改為「公假」 | A＝公假、B＝病假；A 標示覆寫 |
| V4 | 先公假 → 改病假 → 最後掃碼成功 | 歷程新→舊：`準時報到` → `病假` → `公假`；主列時間＝最後一次 |
| V5 | 關閉 batch 掃碼 | 不可報到（既有）；仍可從紀錄填未到原因 |

### 5.6 實作順序（核准後）

1. Migration（batch absence 表＋events 欄位）  
2. 列表／套用 API＋pytest  
3. events 降冪＋stats `latest_event_time`  
4. 前端合併報到紀錄＋批次填原因  
5. 單計畫 Modal 銜接＋歷程 UI  
6. 文件與本機部署驗收步驟  
7. commit／push；**使用者本機 pull 部署驗收**

### 5.7 本機部署（驗收環境）

```bash
# 範例：開發模式
git fetch origin
git checkout <核准後之 feature branch>
git pull

# 後端 migration（依交付文件實際腳本名）
cd backend && .venv/bin/python migrations/<本波腳本>.py
.venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 前端
cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 5173
```

Docker 環境則依 `生產部署指南.md`：`build --no-cache` + `force-recreate` + `docker compose exec … migrations/…`。

---

## 6. 參考文件

| 文件 | 用途 |
|------|------|
| [20260720_合併報到批次與報到歷程_PLAN.md](20260720_合併報到批次與報到歷程_PLAN.md) | 前置合併報到 |
| [../交付實作文件/20260721_合併報到批次與報到歷程.md](../交付實作文件/20260721_合併報到批次與報到歷程.md) | 已交付摘要 |
| [../../00-專案總覽/生產部署指南.md](../../00-專案總覽/生產部署指南.md) | 本機／Docker 部署 |
| [../../00-專案總覽/資料庫遷移/MIGRATION_GUIDE.md](../../00-專案總覽/資料庫遷移/MIGRATION_GUIDE.md) | migration 慣例 |
| `backend/app/models.py` | `AttendanceAbsenceReason`、`AttendanceCheckinEvent`、`AttendanceCheckinBatch` |
| `frontend/src/components/attendance/AttendanceOverviewPage.tsx` | 現有填原因／歷程 UI |

---

## 7. 使用表單（欄位說明）

### 7.1 合併報到紀錄列表（UI）

| 欄位 | 必填 | 說明 |
|------|------|------|
| 場次／報到日 | 否（篩選） | 對應 `batch.training_date` |
| 關鍵字（標籤） | 否（篩選） | 對應 `batch.label` |
| 狀態 | 顯示 | open／closed／reopened |
| 含計畫 | 顯示 | batch 綁定之計畫標題列表 |
| 未到摘要 | 顯示 | 可選：合計未到人數 |

### 7.2 批次未到原因（寫入）

| 欄位 | 必填 | 說明 |
|------|------|------|
| `batch_id` | 是 | 路徑參數 |
| `emp_id` / `emp_ids` | 是 | 單人／批次 |
| `reason_code` | 是 | `sick_leave`／`business_trip`／`official_leave`／`other`／`cancel_leave` |
| `reason_text` | 條件 | `other` 時必填 |

### 7.3 歷程列（顯示）

| 欄位 | 說明 |
|------|------|
| 時間 | `event_time`（新→舊） |
| 狀態文案 | 準時報到／病假／公假／已報到過… |
| 來源（次要） | 合併報到「{標籤}」／計畫 QR／個別覆寫 |

---

## 審核回覆區（請填）

- [ ] PLAN 整體 OK，可建立 TASKS 並實作  
- [ ] 需修改（請註明 Q# 或章節）：  
- [ ] 否決／暫緩：  

**審核人**：____________　**日期**：____________
