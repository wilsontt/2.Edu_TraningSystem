server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Vite base 為 /training/；無結尾斜線時重整會 404／白頁，強制導向有斜線
    location = /training {
        return 302 /training/;
    }

    # SPA fallback：非實體檔案路徑一律回傳 index.html，由前端 Router 處理
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 靜態資源快取
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
