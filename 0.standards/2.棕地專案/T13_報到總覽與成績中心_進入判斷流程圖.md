# T13 報到總覽與成績中心進入判斷流程圖

## 1) 進入「報到總覽」判斷邏輯

```mermaid
flowchart TD
  userLogin[UserLogin] --> openAttendance["Open /attendance-overview"]
  openAttendance --> checkRoute{"role=Admin OR functions includes menu:attendance-overview"}
  checkRoute -->|No| redirectHome["Navigate /"]
  checkRoute -->|Yes| renderAttendance["Render AttendanceOverviewPage"]

  renderAttendance --> callPlans["GET /training/plans?status=active"]
  callPlans --> backendPermission{"check_any_permission(menu:plan,menu:attendance-overview)"}
  backendPermission -->|No| response403["403 Forbidden"]
  backendPermission -->|Yes| scopeFilter["get_scope_emp_ids + training plan audience intersection"]
  scopeFilter --> planList["Return visible training plans only"]
  planList --> callStats["GET /training/plans/{id}/attendance/stats"]
  callStats --> statsScope["get_scope_emp_ids for checked_in/expected/not_checked_in"]
  statsScope --> showTable["Show attendance overview table"]
```

## 2) 進入「成績中心」判斷邏輯

```mermaid
flowchart TD
  userLogin2[UserLogin] --> openReports["Open /reports"]
  openReports --> routeReports["Route -> PersonalScorePage"]
  routeReports --> getMe["GET /auth/me"]
  getMe --> parseMe["Read role/functions/role_scope_type"]

  parseMe --> hasTeamTab{"role=Admin OR (functions includes menu:report AND role_scope_type=department)"}
  hasTeamTab -->|No| hideTeam["Hide tab: 部門成績"]
  hasTeamTab -->|Yes| showTeam["Show tab: 部門成績"]

  hideTeam --> personalTabs["Only: 總覽/歷史記錄/學習分析"]
  showTeam --> clickTeam{"User clicks 部門成績?"}
  clickTeam -->|No| personalTabs
  clickTeam -->|Yes| renderTeam["Render ReportDashboard"]

  renderTeam --> reportApis["Call /admin/reports/* APIs"]
  reportApis --> reportPermission{"check_permission(menu:report)"}
  reportPermission -->|No| report403["403 Forbidden"]
  reportPermission -->|Yes| reportScope["get_scope_emp_ids -> apply emp scope to report queries"]
  reportScope --> reportData["Show department/plan score data by allowed scope"]
```

## 3) 核心規則摘要

- 報到總覽：入口權限由前端路由先判斷，資料可視範圍由後端 `get_scope_emp_ids` 決定。
- 成績中心：`/reports` 一律可進入個人頁；`部門成績` 頁籤是否顯示，取決於 `Admin` 或 `menu:report + role_scope_type=department`。
- 真正資料範圍最終都由後端 scope 套用，不以前端顯示為唯一依據。
